/* eslint-disable */
// Code generated by scripts/generate-protocol.mjs; DO NOT EDIT.
// Canonical schema SHA-256: 99cde09b07680e874ce1c882422027a3067424d556b367235d759858002eeab0
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $Array = $util.global.Array;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const protocol = $root.protocol = (() => {

    /**
     * Namespace protocol.
     * @exports protocol
     * @namespace
     */
    const protocol = {};

    protocol.CardInfo = (function() {

        /**
         * Properties of a CardInfo.
         * @typedef {Object} protocol.CardInfo.$Properties
         * @property {number|null} [suit] CardInfo suit
         * @property {number|null} [rank] CardInfo rank
         * @property {number|null} [color] CardInfo color
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CardInfo.
         * @memberof protocol
         * @interface ICardInfo
         * @augments protocol.CardInfo.$Properties
         * @deprecated Use protocol.CardInfo.$Properties instead.
         */

        /**
         * Shape of a CardInfo.
         * @typedef {protocol.CardInfo.$Properties} protocol.CardInfo.$Shape
         */

        /**
         * Constructs a new CardInfo.
         * @memberof protocol
         * @classdesc Represents a CardInfo.
         * @constructor
         * @param {protocol.CardInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CardInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CardInfo suit.
         * @member {number} suit
         * @memberof protocol.CardInfo
         * @instance
         */
        CardInfo.prototype.suit = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CardInfo rank.
         * @member {number} rank
         * @memberof protocol.CardInfo
         * @instance
         */
        CardInfo.prototype.rank = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CardInfo color.
         * @member {number} color
         * @memberof protocol.CardInfo
         * @instance
         */
        CardInfo.prototype.color = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified CardInfo message. Does not implicitly {@link protocol.CardInfo.verify|verify} messages.
         * @function encode
         * @memberof protocol.CardInfo
         * @static
         * @param {protocol.CardInfo.$Properties} message CardInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CardInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.suit != null && $Object.hasOwnProperty.call(message, "suit") && (typeof message.suit === "object" ? message.suit.low || message.suit.high : message.suit !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.suit);
            if (message.rank != null && $Object.hasOwnProperty.call(message, "rank") && (typeof message.rank === "object" ? message.rank.low || message.rank.high : message.rank !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.rank);
            if (message.color != null && $Object.hasOwnProperty.call(message, "color") && (typeof message.color === "object" ? message.color.low || message.color.high : message.color !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.color);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a CardInfo message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.CardInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.CardInfo & protocol.CardInfo.$Shape} CardInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CardInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.CardInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.suit = value;
                        else
                            delete message.suit;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.rank = value;
                        else
                            delete message.rank;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.color = value;
                        else
                            delete message.color;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a CardInfo message.
         * @function verify
         * @memberof protocol.CardInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CardInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.suit != null && $Object.hasOwnProperty.call(message, "suit"))
                if (!$util.isInteger(message.suit) && !(message.suit && $util.isInteger(message.suit.low) && $util.isInteger(message.suit.high)))
                    return "suit: integer|Long expected";
            if (message.rank != null && $Object.hasOwnProperty.call(message, "rank"))
                if (!$util.isInteger(message.rank) && !(message.rank && $util.isInteger(message.rank.low) && $util.isInteger(message.rank.high)))
                    return "rank: integer|Long expected";
            if (message.color != null && $Object.hasOwnProperty.call(message, "color"))
                if (!$util.isInteger(message.color) && !(message.color && $util.isInteger(message.color.low) && $util.isInteger(message.color.high)))
                    return "color: integer|Long expected";
            return null;
        };

        return CardInfo;
    })();

    protocol.PlayerInfo = (function() {

        /**
         * Properties of a PlayerInfo.
         * @typedef {Object} protocol.PlayerInfo.$Properties
         * @property {string|null} [id] PlayerInfo id
         * @property {string|null} [name] PlayerInfo name
         * @property {number|null} [seat] PlayerInfo seat
         * @property {boolean|null} [ready] PlayerInfo ready
         * @property {boolean|null} [is_landlord] PlayerInfo is_landlord
         * @property {number|null} [cards_count] PlayerInfo cards_count
         * @property {boolean|null} [online] PlayerInfo online
         * @property {boolean|null} [is_bot] PlayerInfo is_bot
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerInfo.
         * @memberof protocol
         * @interface IPlayerInfo
         * @augments protocol.PlayerInfo.$Properties
         * @deprecated Use protocol.PlayerInfo.$Properties instead.
         */

        /**
         * Shape of a PlayerInfo.
         * @typedef {protocol.PlayerInfo.$Properties} protocol.PlayerInfo.$Shape
         */

        /**
         * Constructs a new PlayerInfo.
         * @memberof protocol
         * @classdesc Represents a PlayerInfo.
         * @constructor
         * @param {protocol.PlayerInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerInfo id.
         * @member {string} id
         * @memberof protocol.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.id = "";

        /**
         * PlayerInfo name.
         * @member {string} name
         * @memberof protocol.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.name = "";

        /**
         * PlayerInfo seat.
         * @member {number} seat
         * @memberof protocol.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.seat = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayerInfo ready.
         * @member {boolean} ready
         * @memberof protocol.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.ready = false;

        /**
         * PlayerInfo is_landlord.
         * @member {boolean} is_landlord
         * @memberof protocol.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.is_landlord = false;

        /**
         * PlayerInfo cards_count.
         * @member {number} cards_count
         * @memberof protocol.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.cards_count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayerInfo online.
         * @member {boolean} online
         * @memberof protocol.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.online = false;

        /**
         * PlayerInfo is_bot.
         * @member {boolean} is_bot
         * @memberof protocol.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.is_bot = false;

        /**
         * Encodes the specified PlayerInfo message. Does not implicitly {@link protocol.PlayerInfo.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerInfo
         * @static
         * @param {protocol.PlayerInfo.$Properties} message PlayerInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.seat != null && $Object.hasOwnProperty.call(message, "seat") && (typeof message.seat === "object" ? message.seat.low || message.seat.high : message.seat !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.seat);
            if (message.ready != null && $Object.hasOwnProperty.call(message, "ready") && message.ready !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.ready);
            if (message.is_landlord != null && $Object.hasOwnProperty.call(message, "is_landlord") && message.is_landlord !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.is_landlord);
            if (message.cards_count != null && $Object.hasOwnProperty.call(message, "cards_count") && (typeof message.cards_count === "object" ? message.cards_count.low || message.cards_count.high : message.cards_count !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.cards_count);
            if (message.online != null && $Object.hasOwnProperty.call(message, "online") && message.online !== false)
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.online);
            if (message.is_bot != null && $Object.hasOwnProperty.call(message, "is_bot") && message.is_bot !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.is_bot);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerInfo message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerInfo & protocol.PlayerInfo.$Shape} PlayerInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ready = value;
                        else
                            delete message.ready;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_landlord = value;
                        else
                            delete message.is_landlord;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.cards_count = value;
                        else
                            delete message.cards_count;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.online = value;
                        else
                            delete message.online;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_bot = value;
                        else
                            delete message.is_bot;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerInfo message.
         * @function verify
         * @memberof protocol.PlayerInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.seat != null && $Object.hasOwnProperty.call(message, "seat"))
                if (!$util.isInteger(message.seat) && !(message.seat && $util.isInteger(message.seat.low) && $util.isInteger(message.seat.high)))
                    return "seat: integer|Long expected";
            if (message.ready != null && $Object.hasOwnProperty.call(message, "ready"))
                if (typeof message.ready !== "boolean")
                    return "ready: boolean expected";
            if (message.is_landlord != null && $Object.hasOwnProperty.call(message, "is_landlord"))
                if (typeof message.is_landlord !== "boolean")
                    return "is_landlord: boolean expected";
            if (message.cards_count != null && $Object.hasOwnProperty.call(message, "cards_count"))
                if (!$util.isInteger(message.cards_count) && !(message.cards_count && $util.isInteger(message.cards_count.low) && $util.isInteger(message.cards_count.high)))
                    return "cards_count: integer|Long expected";
            if (message.online != null && $Object.hasOwnProperty.call(message, "online"))
                if (typeof message.online !== "boolean")
                    return "online: boolean expected";
            if (message.is_bot != null && $Object.hasOwnProperty.call(message, "is_bot"))
                if (typeof message.is_bot !== "boolean")
                    return "is_bot: boolean expected";
            return null;
        };

        return PlayerInfo;
    })();

    protocol.PlayerPlayedCards = (function() {

        /**
         * Properties of a PlayerPlayedCards.
         * @typedef {Object} protocol.PlayerPlayedCards.$Properties
         * @property {string|null} [player_id] PlayerPlayedCards player_id
         * @property {Array.<protocol.CardInfo.$Properties>|null} [cards] PlayerPlayedCards cards
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerPlayedCards.
         * @memberof protocol
         * @interface IPlayerPlayedCards
         * @augments protocol.PlayerPlayedCards.$Properties
         * @deprecated Use protocol.PlayerPlayedCards.$Properties instead.
         */

        /**
         * Shape of a PlayerPlayedCards.
         * @typedef {protocol.PlayerPlayedCards.$Properties} protocol.PlayerPlayedCards.$Shape
         */

        /**
         * Constructs a new PlayerPlayedCards.
         * @memberof protocol
         * @classdesc Represents a PlayerPlayedCards.
         * @constructor
         * @param {protocol.PlayerPlayedCards.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerPlayedCards = function (properties) {
            this.cards = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerPlayedCards player_id.
         * @member {string} player_id
         * @memberof protocol.PlayerPlayedCards
         * @instance
         */
        PlayerPlayedCards.prototype.player_id = "";

        /**
         * PlayerPlayedCards cards.
         * @member {Array.<protocol.CardInfo.$Properties>} cards
         * @memberof protocol.PlayerPlayedCards
         * @instance
         */
        PlayerPlayedCards.prototype.cards = $util.emptyArray;

        /**
         * Encodes the specified PlayerPlayedCards message. Does not implicitly {@link protocol.PlayerPlayedCards.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerPlayedCards
         * @static
         * @param {protocol.PlayerPlayedCards.$Properties} message PlayerPlayedCards message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerPlayedCards.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.cards != null && message.cards.length)
                for (let i = 0; i < message.cards.length; ++i)
                    $root.protocol.CardInfo.encode(message.cards[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerPlayedCards message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerPlayedCards
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerPlayedCards & protocol.PlayerPlayedCards.$Shape} PlayerPlayedCards
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerPlayedCards.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerPlayedCards(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerPlayedCards message.
         * @function verify
         * @memberof protocol.PlayerPlayedCards
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerPlayedCards.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.cards != null && $Object.hasOwnProperty.call(message, "cards")) {
                if (!$Array.isArray(message.cards))
                    return "cards: array expected";
                for (let i = 0; i < message.cards.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.cards[i], _depth + 1);
                    if (error)
                        return "cards." + error;
                }
            }
            return null;
        };

        return PlayerPlayedCards;
    })();

    protocol.PlayerHand = (function() {

        /**
         * Properties of a PlayerHand.
         * @typedef {Object} protocol.PlayerHand.$Properties
         * @property {string|null} [player_id] PlayerHand player_id
         * @property {string|null} [player_name] PlayerHand player_name
         * @property {Array.<protocol.CardInfo.$Properties>|null} [cards] PlayerHand cards
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerHand.
         * @memberof protocol
         * @interface IPlayerHand
         * @augments protocol.PlayerHand.$Properties
         * @deprecated Use protocol.PlayerHand.$Properties instead.
         */

        /**
         * Shape of a PlayerHand.
         * @typedef {protocol.PlayerHand.$Properties} protocol.PlayerHand.$Shape
         */

        /**
         * Constructs a new PlayerHand.
         * @memberof protocol
         * @classdesc Represents a PlayerHand.
         * @constructor
         * @param {protocol.PlayerHand.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerHand = function (properties) {
            this.cards = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerHand player_id.
         * @member {string} player_id
         * @memberof protocol.PlayerHand
         * @instance
         */
        PlayerHand.prototype.player_id = "";

        /**
         * PlayerHand player_name.
         * @member {string} player_name
         * @memberof protocol.PlayerHand
         * @instance
         */
        PlayerHand.prototype.player_name = "";

        /**
         * PlayerHand cards.
         * @member {Array.<protocol.CardInfo.$Properties>} cards
         * @memberof protocol.PlayerHand
         * @instance
         */
        PlayerHand.prototype.cards = $util.emptyArray;

        /**
         * Encodes the specified PlayerHand message. Does not implicitly {@link protocol.PlayerHand.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerHand
         * @static
         * @param {protocol.PlayerHand.$Properties} message PlayerHand message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerHand.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.cards != null && message.cards.length)
                for (let i = 0; i < message.cards.length; ++i)
                    $root.protocol.CardInfo.encode(message.cards[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerHand message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerHand
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerHand & protocol.PlayerHand.$Shape} PlayerHand
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerHand.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerHand(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerHand message.
         * @function verify
         * @memberof protocol.PlayerHand
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerHand.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.cards != null && $Object.hasOwnProperty.call(message, "cards")) {
                if (!$Array.isArray(message.cards))
                    return "cards: array expected";
                for (let i = 0; i < message.cards.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.cards[i], _depth + 1);
                    if (error)
                        return "cards." + error;
                }
            }
            return null;
        };

        return PlayerHand;
    })();

    protocol.PlayerScore = (function() {

        /**
         * Properties of a PlayerScore.
         * @typedef {Object} protocol.PlayerScore.$Properties
         * @property {string|null} [player_id] PlayerScore player_id
         * @property {string|null} [player_name] PlayerScore player_name
         * @property {boolean|null} [is_landlord] PlayerScore is_landlord
         * @property {number|null} [score] PlayerScore score
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerScore.
         * @memberof protocol
         * @interface IPlayerScore
         * @augments protocol.PlayerScore.$Properties
         * @deprecated Use protocol.PlayerScore.$Properties instead.
         */

        /**
         * Shape of a PlayerScore.
         * @typedef {protocol.PlayerScore.$Properties} protocol.PlayerScore.$Shape
         */

        /**
         * Constructs a new PlayerScore.
         * @memberof protocol
         * @classdesc Represents a PlayerScore.
         * @constructor
         * @param {protocol.PlayerScore.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerScore = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerScore player_id.
         * @member {string} player_id
         * @memberof protocol.PlayerScore
         * @instance
         */
        PlayerScore.prototype.player_id = "";

        /**
         * PlayerScore player_name.
         * @member {string} player_name
         * @memberof protocol.PlayerScore
         * @instance
         */
        PlayerScore.prototype.player_name = "";

        /**
         * PlayerScore is_landlord.
         * @member {boolean} is_landlord
         * @memberof protocol.PlayerScore
         * @instance
         */
        PlayerScore.prototype.is_landlord = false;

        /**
         * PlayerScore score.
         * @member {number} score
         * @memberof protocol.PlayerScore
         * @instance
         */
        PlayerScore.prototype.score = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified PlayerScore message. Does not implicitly {@link protocol.PlayerScore.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerScore
         * @static
         * @param {protocol.PlayerScore.$Properties} message PlayerScore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerScore.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.is_landlord != null && $Object.hasOwnProperty.call(message, "is_landlord") && message.is_landlord !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.is_landlord);
            if (message.score != null && $Object.hasOwnProperty.call(message, "score") && (typeof message.score === "object" ? message.score.low || message.score.high : message.score !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.score);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerScore message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerScore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerScore & protocol.PlayerScore.$Shape} PlayerScore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerScore.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerScore(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_landlord = value;
                        else
                            delete message.is_landlord;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerScore message.
         * @function verify
         * @memberof protocol.PlayerScore
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerScore.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.is_landlord != null && $Object.hasOwnProperty.call(message, "is_landlord"))
                if (typeof message.is_landlord !== "boolean")
                    return "is_landlord: boolean expected";
            if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
                if (!$util.isInteger(message.score) && !(message.score && $util.isInteger(message.score.low) && $util.isInteger(message.score.high)))
                    return "score: integer|Long expected";
            return null;
        };

        return PlayerScore;
    })();

    protocol.GameSettlementDTO = (function() {

        /**
         * Properties of a GameSettlementDTO.
         * @typedef {Object} protocol.GameSettlementDTO.$Properties
         * @property {string|null} [winner_id] GameSettlementDTO winner_id
         * @property {string|null} [winner_name] GameSettlementDTO winner_name
         * @property {boolean|null} [winner_is_landlord] GameSettlementDTO winner_is_landlord
         * @property {number|null} [multiplier] GameSettlementDTO multiplier
         * @property {Array.<protocol.PlayerScore.$Properties>|null} [scores] GameSettlementDTO scores
         * @property {Array.<protocol.PlayerHand.$Properties>|null} [player_hands] GameSettlementDTO player_hands
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GameSettlementDTO.
         * @memberof protocol
         * @interface IGameSettlementDTO
         * @augments protocol.GameSettlementDTO.$Properties
         * @deprecated Use protocol.GameSettlementDTO.$Properties instead.
         */

        /**
         * Shape of a GameSettlementDTO.
         * @typedef {protocol.GameSettlementDTO.$Properties} protocol.GameSettlementDTO.$Shape
         */

        /**
         * Constructs a new GameSettlementDTO.
         * @memberof protocol
         * @classdesc Represents a GameSettlementDTO.
         * @constructor
         * @param {protocol.GameSettlementDTO.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GameSettlementDTO = function (properties) {
            this.scores = [];
            this.player_hands = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GameSettlementDTO winner_id.
         * @member {string} winner_id
         * @memberof protocol.GameSettlementDTO
         * @instance
         */
        GameSettlementDTO.prototype.winner_id = "";

        /**
         * GameSettlementDTO winner_name.
         * @member {string} winner_name
         * @memberof protocol.GameSettlementDTO
         * @instance
         */
        GameSettlementDTO.prototype.winner_name = "";

        /**
         * GameSettlementDTO winner_is_landlord.
         * @member {boolean} winner_is_landlord
         * @memberof protocol.GameSettlementDTO
         * @instance
         */
        GameSettlementDTO.prototype.winner_is_landlord = false;

        /**
         * GameSettlementDTO multiplier.
         * @member {number} multiplier
         * @memberof protocol.GameSettlementDTO
         * @instance
         */
        GameSettlementDTO.prototype.multiplier = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GameSettlementDTO scores.
         * @member {Array.<protocol.PlayerScore.$Properties>} scores
         * @memberof protocol.GameSettlementDTO
         * @instance
         */
        GameSettlementDTO.prototype.scores = $util.emptyArray;

        /**
         * GameSettlementDTO player_hands.
         * @member {Array.<protocol.PlayerHand.$Properties>} player_hands
         * @memberof protocol.GameSettlementDTO
         * @instance
         */
        GameSettlementDTO.prototype.player_hands = $util.emptyArray;

        /**
         * Encodes the specified GameSettlementDTO message. Does not implicitly {@link protocol.GameSettlementDTO.verify|verify} messages.
         * @function encode
         * @memberof protocol.GameSettlementDTO
         * @static
         * @param {protocol.GameSettlementDTO.$Properties} message GameSettlementDTO message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameSettlementDTO.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.winner_id != null && $Object.hasOwnProperty.call(message, "winner_id") && message.winner_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.winner_id);
            if (message.winner_name != null && $Object.hasOwnProperty.call(message, "winner_name") && message.winner_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.winner_name);
            if (message.winner_is_landlord != null && $Object.hasOwnProperty.call(message, "winner_is_landlord") && message.winner_is_landlord !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.winner_is_landlord);
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier") && (typeof message.multiplier === "object" ? message.multiplier.low || message.multiplier.high : message.multiplier !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.multiplier);
            if (message.scores != null && message.scores.length)
                for (let i = 0; i < message.scores.length; ++i)
                    $root.protocol.PlayerScore.encode(message.scores[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.player_hands != null && message.player_hands.length)
                for (let i = 0; i < message.player_hands.length; ++i)
                    $root.protocol.PlayerHand.encode(message.player_hands[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a GameSettlementDTO message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.GameSettlementDTO
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.GameSettlementDTO & protocol.GameSettlementDTO.$Shape} GameSettlementDTO
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameSettlementDTO.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.GameSettlementDTO(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.winner_id = value;
                        else
                            delete message.winner_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.winner_name = value;
                        else
                            delete message.winner_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.winner_is_landlord = value;
                        else
                            delete message.winner_is_landlord;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.multiplier = value;
                        else
                            delete message.multiplier;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push($root.protocol.PlayerScore.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.player_hands && message.player_hands.length))
                            message.player_hands = [];
                        message.player_hands.push($root.protocol.PlayerHand.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a GameSettlementDTO message.
         * @function verify
         * @memberof protocol.GameSettlementDTO
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameSettlementDTO.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.winner_id != null && $Object.hasOwnProperty.call(message, "winner_id"))
                if (!$util.isString(message.winner_id))
                    return "winner_id: string expected";
            if (message.winner_name != null && $Object.hasOwnProperty.call(message, "winner_name"))
                if (!$util.isString(message.winner_name))
                    return "winner_name: string expected";
            if (message.winner_is_landlord != null && $Object.hasOwnProperty.call(message, "winner_is_landlord"))
                if (typeof message.winner_is_landlord !== "boolean")
                    return "winner_is_landlord: boolean expected";
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier"))
                if (!$util.isInteger(message.multiplier) && !(message.multiplier && $util.isInteger(message.multiplier.low) && $util.isInteger(message.multiplier.high)))
                    return "multiplier: integer|Long expected";
            if (message.scores != null && $Object.hasOwnProperty.call(message, "scores")) {
                if (!$Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i) {
                    let error = $root.protocol.PlayerScore.verify(message.scores[i], _depth + 1);
                    if (error)
                        return "scores." + error;
                }
            }
            if (message.player_hands != null && $Object.hasOwnProperty.call(message, "player_hands")) {
                if (!$Array.isArray(message.player_hands))
                    return "player_hands: array expected";
                for (let i = 0; i < message.player_hands.length; ++i) {
                    let error = $root.protocol.PlayerHand.verify(message.player_hands[i], _depth + 1);
                    if (error)
                        return "player_hands." + error;
                }
            }
            return null;
        };

        return GameSettlementDTO;
    })();

    protocol.GameStateDTO = (function() {

        /**
         * Properties of a GameStateDTO.
         * @typedef {Object} protocol.GameStateDTO.$Properties
         * @property {string|null} [phase] GameStateDTO phase
         * @property {Array.<protocol.PlayerInfo.$Properties>|null} [players] GameStateDTO players
         * @property {Array.<protocol.CardInfo.$Properties>|null} [hand] GameStateDTO hand
         * @property {Array.<protocol.CardInfo.$Properties>|null} [bottom_cards] GameStateDTO bottom_cards
         * @property {string|null} [current_turn] GameStateDTO current_turn
         * @property {Array.<protocol.CardInfo.$Properties>|null} [last_played] GameStateDTO last_played
         * @property {string|null} [last_player_id] GameStateDTO last_player_id
         * @property {boolean|null} [must_play] GameStateDTO must_play
         * @property {boolean|null} [can_beat] GameStateDTO can_beat
         * @property {number|null} [snapshot_version] GameStateDTO snapshot_version
         * @property {string|null} [game_id] GameStateDTO game_id
         * @property {boolean|null} [bottom_cards_revealed] GameStateDTO bottom_cards_revealed
         * @property {number|null} [turn_id] GameStateDTO turn_id
         * @property {number|null} [turn_deadline_ms] GameStateDTO turn_deadline_ms
         * @property {number|null} [server_time_ms] GameStateDTO server_time_ms
         * @property {string|null} [last_player_name] GameStateDTO last_player_name
         * @property {string|null} [last_hand_type] GameStateDTO last_hand_type
         * @property {boolean|null} [is_grab] GameStateDTO is_grab
         * @property {number|null} [multiplier] GameStateDTO multiplier
         * @property {number|null} [base_score] GameStateDTO base_score
         * @property {Array.<protocol.PlayerPlayedCards.$Properties>|null} [played_cards] GameStateDTO played_cards
         * @property {protocol.GameSettlementDTO.$Properties|null} [settlement] GameStateDTO settlement
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GameStateDTO.
         * @memberof protocol
         * @interface IGameStateDTO
         * @augments protocol.GameStateDTO.$Properties
         * @deprecated Use protocol.GameStateDTO.$Properties instead.
         */

        /**
         * Shape of a GameStateDTO.
         * @typedef {protocol.GameStateDTO.$Properties} protocol.GameStateDTO.$Shape
         */

        /**
         * Constructs a new GameStateDTO.
         * @memberof protocol
         * @classdesc Represents a GameStateDTO.
         * @constructor
         * @param {protocol.GameStateDTO.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GameStateDTO = function (properties) {
            this.players = [];
            this.hand = [];
            this.bottom_cards = [];
            this.last_played = [];
            this.played_cards = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GameStateDTO phase.
         * @member {string} phase
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.phase = "";

        /**
         * GameStateDTO players.
         * @member {Array.<protocol.PlayerInfo.$Properties>} players
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.players = $util.emptyArray;

        /**
         * GameStateDTO hand.
         * @member {Array.<protocol.CardInfo.$Properties>} hand
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.hand = $util.emptyArray;

        /**
         * GameStateDTO bottom_cards.
         * @member {Array.<protocol.CardInfo.$Properties>} bottom_cards
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.bottom_cards = $util.emptyArray;

        /**
         * GameStateDTO current_turn.
         * @member {string} current_turn
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.current_turn = "";

        /**
         * GameStateDTO last_played.
         * @member {Array.<protocol.CardInfo.$Properties>} last_played
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.last_played = $util.emptyArray;

        /**
         * GameStateDTO last_player_id.
         * @member {string} last_player_id
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.last_player_id = "";

        /**
         * GameStateDTO must_play.
         * @member {boolean} must_play
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.must_play = false;

        /**
         * GameStateDTO can_beat.
         * @member {boolean} can_beat
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.can_beat = false;

        /**
         * GameStateDTO snapshot_version.
         * @member {number} snapshot_version
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.snapshot_version = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GameStateDTO game_id.
         * @member {string} game_id
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.game_id = "";

        /**
         * GameStateDTO bottom_cards_revealed.
         * @member {boolean} bottom_cards_revealed
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.bottom_cards_revealed = false;

        /**
         * GameStateDTO turn_id.
         * @member {number} turn_id
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.turn_id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GameStateDTO turn_deadline_ms.
         * @member {number} turn_deadline_ms
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.turn_deadline_ms = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GameStateDTO server_time_ms.
         * @member {number} server_time_ms
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.server_time_ms = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GameStateDTO last_player_name.
         * @member {string} last_player_name
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.last_player_name = "";

        /**
         * GameStateDTO last_hand_type.
         * @member {string} last_hand_type
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.last_hand_type = "";

        /**
         * GameStateDTO is_grab.
         * @member {boolean} is_grab
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.is_grab = false;

        /**
         * GameStateDTO multiplier.
         * @member {number} multiplier
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.multiplier = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GameStateDTO base_score.
         * @member {number} base_score
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.base_score = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GameStateDTO played_cards.
         * @member {Array.<protocol.PlayerPlayedCards.$Properties>} played_cards
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.played_cards = $util.emptyArray;

        /**
         * GameStateDTO settlement.
         * @member {protocol.GameSettlementDTO.$Properties|null|undefined} settlement
         * @memberof protocol.GameStateDTO
         * @instance
         */
        GameStateDTO.prototype.settlement = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GameStateDTO.prototype, "_settlement", {
            get: $util.oneOfGetter($oneOfFields = ["settlement"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Encodes the specified GameStateDTO message. Does not implicitly {@link protocol.GameStateDTO.verify|verify} messages.
         * @function encode
         * @memberof protocol.GameStateDTO
         * @static
         * @param {protocol.GameStateDTO.$Properties} message GameStateDTO message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameStateDTO.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.phase != null && $Object.hasOwnProperty.call(message, "phase") && message.phase !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.phase);
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.protocol.PlayerInfo.encode(message.players[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.hand != null && message.hand.length)
                for (let i = 0; i < message.hand.length; ++i)
                    $root.protocol.CardInfo.encode(message.hand[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.bottom_cards != null && message.bottom_cards.length)
                for (let i = 0; i < message.bottom_cards.length; ++i)
                    $root.protocol.CardInfo.encode(message.bottom_cards[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.current_turn != null && $Object.hasOwnProperty.call(message, "current_turn") && message.current_turn !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.current_turn);
            if (message.last_played != null && message.last_played.length)
                for (let i = 0; i < message.last_played.length; ++i)
                    $root.protocol.CardInfo.encode(message.last_played[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.last_player_id != null && $Object.hasOwnProperty.call(message, "last_player_id") && message.last_player_id !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.last_player_id);
            if (message.must_play != null && $Object.hasOwnProperty.call(message, "must_play") && message.must_play !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.must_play);
            if (message.can_beat != null && $Object.hasOwnProperty.call(message, "can_beat") && message.can_beat !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.can_beat);
            if (message.snapshot_version != null && $Object.hasOwnProperty.call(message, "snapshot_version") && (typeof message.snapshot_version === "object" ? message.snapshot_version.low || message.snapshot_version.high : message.snapshot_version !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.snapshot_version);
            if (message.game_id != null && $Object.hasOwnProperty.call(message, "game_id") && message.game_id !== "")
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.game_id);
            if (message.bottom_cards_revealed != null && $Object.hasOwnProperty.call(message, "bottom_cards_revealed") && message.bottom_cards_revealed !== false)
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.bottom_cards_revealed);
            if (message.turn_id != null && $Object.hasOwnProperty.call(message, "turn_id") && (typeof message.turn_id === "object" ? message.turn_id.low || message.turn_id.high : message.turn_id !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.turn_id);
            if (message.turn_deadline_ms != null && $Object.hasOwnProperty.call(message, "turn_deadline_ms") && (typeof message.turn_deadline_ms === "object" ? message.turn_deadline_ms.low || message.turn_deadline_ms.high : message.turn_deadline_ms !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.turn_deadline_ms);
            if (message.server_time_ms != null && $Object.hasOwnProperty.call(message, "server_time_ms") && (typeof message.server_time_ms === "object" ? message.server_time_ms.low || message.server_time_ms.high : message.server_time_ms !== 0))
                writer.uint32(/* id 15, wireType 0 =*/120).int64(message.server_time_ms);
            if (message.last_player_name != null && $Object.hasOwnProperty.call(message, "last_player_name") && message.last_player_name !== "")
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.last_player_name);
            if (message.last_hand_type != null && $Object.hasOwnProperty.call(message, "last_hand_type") && message.last_hand_type !== "")
                writer.uint32(/* id 17, wireType 2 =*/138).string(message.last_hand_type);
            if (message.is_grab != null && $Object.hasOwnProperty.call(message, "is_grab") && message.is_grab !== false)
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.is_grab);
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier") && (typeof message.multiplier === "object" ? message.multiplier.low || message.multiplier.high : message.multiplier !== 0))
                writer.uint32(/* id 19, wireType 0 =*/152).int64(message.multiplier);
            if (message.base_score != null && $Object.hasOwnProperty.call(message, "base_score") && (typeof message.base_score === "object" ? message.base_score.low || message.base_score.high : message.base_score !== 0))
                writer.uint32(/* id 20, wireType 0 =*/160).int64(message.base_score);
            if (message.played_cards != null && message.played_cards.length)
                for (let i = 0; i < message.played_cards.length; ++i)
                    $root.protocol.PlayerPlayedCards.encode(message.played_cards[i], writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
            if (message.settlement != null && $Object.hasOwnProperty.call(message, "settlement"))
                $root.protocol.GameSettlementDTO.encode(message.settlement, writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a GameStateDTO message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.GameStateDTO
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.GameStateDTO & protocol.GameStateDTO.$Shape} GameStateDTO
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameStateDTO.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.GameStateDTO(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.phase = value;
                        else
                            delete message.phase;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.protocol.PlayerInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hand && message.hand.length))
                            message.hand = [];
                        message.hand.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bottom_cards && message.bottom_cards.length))
                            message.bottom_cards = [];
                        message.bottom_cards.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.current_turn = value;
                        else
                            delete message.current_turn;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.last_played && message.last_played.length))
                            message.last_played = [];
                        message.last_played.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.last_player_id = value;
                        else
                            delete message.last_player_id;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.must_play = value;
                        else
                            delete message.must_play;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.can_beat = value;
                        else
                            delete message.can_beat;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.snapshot_version = value;
                        else
                            delete message.snapshot_version;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.game_id = value;
                        else
                            delete message.game_id;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.bottom_cards_revealed = value;
                        else
                            delete message.bottom_cards_revealed;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.turn_id = value;
                        else
                            delete message.turn_id;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.turn_deadline_ms = value;
                        else
                            delete message.turn_deadline_ms;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.server_time_ms = value;
                        else
                            delete message.server_time_ms;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.last_player_name = value;
                        else
                            delete message.last_player_name;
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.last_hand_type = value;
                        else
                            delete message.last_hand_type;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_grab = value;
                        else
                            delete message.is_grab;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.multiplier = value;
                        else
                            delete message.multiplier;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.base_score = value;
                        else
                            delete message.base_score;
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if (!(message.played_cards && message.played_cards.length))
                            message.played_cards = [];
                        message.played_cards.push($root.protocol.PlayerPlayedCards.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.settlement = $root.protocol.GameSettlementDTO.decode(reader, reader.uint32(), $undefined, _depth + 1, message.settlement);
                        message._settlement = "settlement";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a GameStateDTO message.
         * @function verify
         * @memberof protocol.GameStateDTO
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameStateDTO.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                if (!$util.isString(message.phase))
                    return "phase: string expected";
            if (message.players != null && $Object.hasOwnProperty.call(message, "players")) {
                if (!$Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.protocol.PlayerInfo.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.hand != null && $Object.hasOwnProperty.call(message, "hand")) {
                if (!$Array.isArray(message.hand))
                    return "hand: array expected";
                for (let i = 0; i < message.hand.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.hand[i], _depth + 1);
                    if (error)
                        return "hand." + error;
                }
            }
            if (message.bottom_cards != null && $Object.hasOwnProperty.call(message, "bottom_cards")) {
                if (!$Array.isArray(message.bottom_cards))
                    return "bottom_cards: array expected";
                for (let i = 0; i < message.bottom_cards.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.bottom_cards[i], _depth + 1);
                    if (error)
                        return "bottom_cards." + error;
                }
            }
            if (message.current_turn != null && $Object.hasOwnProperty.call(message, "current_turn"))
                if (!$util.isString(message.current_turn))
                    return "current_turn: string expected";
            if (message.last_played != null && $Object.hasOwnProperty.call(message, "last_played")) {
                if (!$Array.isArray(message.last_played))
                    return "last_played: array expected";
                for (let i = 0; i < message.last_played.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.last_played[i], _depth + 1);
                    if (error)
                        return "last_played." + error;
                }
            }
            if (message.last_player_id != null && $Object.hasOwnProperty.call(message, "last_player_id"))
                if (!$util.isString(message.last_player_id))
                    return "last_player_id: string expected";
            if (message.must_play != null && $Object.hasOwnProperty.call(message, "must_play"))
                if (typeof message.must_play !== "boolean")
                    return "must_play: boolean expected";
            if (message.can_beat != null && $Object.hasOwnProperty.call(message, "can_beat"))
                if (typeof message.can_beat !== "boolean")
                    return "can_beat: boolean expected";
            if (message.snapshot_version != null && $Object.hasOwnProperty.call(message, "snapshot_version"))
                if (!$util.isInteger(message.snapshot_version) && !(message.snapshot_version && $util.isInteger(message.snapshot_version.low) && $util.isInteger(message.snapshot_version.high)))
                    return "snapshot_version: integer|Long expected";
            if (message.game_id != null && $Object.hasOwnProperty.call(message, "game_id"))
                if (!$util.isString(message.game_id))
                    return "game_id: string expected";
            if (message.bottom_cards_revealed != null && $Object.hasOwnProperty.call(message, "bottom_cards_revealed"))
                if (typeof message.bottom_cards_revealed !== "boolean")
                    return "bottom_cards_revealed: boolean expected";
            if (message.turn_id != null && $Object.hasOwnProperty.call(message, "turn_id"))
                if (!$util.isInteger(message.turn_id) && !(message.turn_id && $util.isInteger(message.turn_id.low) && $util.isInteger(message.turn_id.high)))
                    return "turn_id: integer|Long expected";
            if (message.turn_deadline_ms != null && $Object.hasOwnProperty.call(message, "turn_deadline_ms"))
                if (!$util.isInteger(message.turn_deadline_ms) && !(message.turn_deadline_ms && $util.isInteger(message.turn_deadline_ms.low) && $util.isInteger(message.turn_deadline_ms.high)))
                    return "turn_deadline_ms: integer|Long expected";
            if (message.server_time_ms != null && $Object.hasOwnProperty.call(message, "server_time_ms"))
                if (!$util.isInteger(message.server_time_ms) && !(message.server_time_ms && $util.isInteger(message.server_time_ms.low) && $util.isInteger(message.server_time_ms.high)))
                    return "server_time_ms: integer|Long expected";
            if (message.last_player_name != null && $Object.hasOwnProperty.call(message, "last_player_name"))
                if (!$util.isString(message.last_player_name))
                    return "last_player_name: string expected";
            if (message.last_hand_type != null && $Object.hasOwnProperty.call(message, "last_hand_type"))
                if (!$util.isString(message.last_hand_type))
                    return "last_hand_type: string expected";
            if (message.is_grab != null && $Object.hasOwnProperty.call(message, "is_grab"))
                if (typeof message.is_grab !== "boolean")
                    return "is_grab: boolean expected";
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier"))
                if (!$util.isInteger(message.multiplier) && !(message.multiplier && $util.isInteger(message.multiplier.low) && $util.isInteger(message.multiplier.high)))
                    return "multiplier: integer|Long expected";
            if (message.base_score != null && $Object.hasOwnProperty.call(message, "base_score"))
                if (!$util.isInteger(message.base_score) && !(message.base_score && $util.isInteger(message.base_score.low) && $util.isInteger(message.base_score.high)))
                    return "base_score: integer|Long expected";
            if (message.played_cards != null && $Object.hasOwnProperty.call(message, "played_cards")) {
                if (!$Array.isArray(message.played_cards))
                    return "played_cards: array expected";
                for (let i = 0; i < message.played_cards.length; ++i) {
                    let error = $root.protocol.PlayerPlayedCards.verify(message.played_cards[i], _depth + 1);
                    if (error)
                        return "played_cards." + error;
                }
            }
            if (message.settlement != null && $Object.hasOwnProperty.call(message, "settlement")) {
                properties._settlement = 1;
                {
                    let error = $root.protocol.GameSettlementDTO.verify(message.settlement, _depth + 1);
                    if (error)
                        return "settlement." + error;
                }
            }
            return null;
        };

        return GameStateDTO;
    })();

    protocol.ChatPayload = (function() {

        /**
         * Properties of a ChatPayload.
         * @typedef {Object} protocol.ChatPayload.$Properties
         * @property {string|null} [sender_id] ChatPayload sender_id
         * @property {string|null} [sender_name] ChatPayload sender_name
         * @property {string|null} [content] ChatPayload content
         * @property {string|null} [scope] ChatPayload scope
         * @property {number|null} [time] ChatPayload time
         * @property {boolean|null} [is_system] ChatPayload is_system
         * @property {string|null} [message_id] ChatPayload message_id
         * @property {string|null} [room_code] ChatPayload room_code
         * @property {string|null} [game_id] ChatPayload game_id
         * @property {number|null} [server_time] ChatPayload server_time
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ChatPayload.
         * @memberof protocol
         * @interface IChatPayload
         * @augments protocol.ChatPayload.$Properties
         * @deprecated Use protocol.ChatPayload.$Properties instead.
         */

        /**
         * Shape of a ChatPayload.
         * @typedef {protocol.ChatPayload.$Properties} protocol.ChatPayload.$Shape
         */

        /**
         * Constructs a new ChatPayload.
         * @memberof protocol
         * @classdesc Represents a ChatPayload.
         * @constructor
         * @param {protocol.ChatPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ChatPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChatPayload sender_id.
         * @member {string} sender_id
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.sender_id = "";

        /**
         * ChatPayload sender_name.
         * @member {string} sender_name
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.sender_name = "";

        /**
         * ChatPayload content.
         * @member {string} content
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.content = "";

        /**
         * ChatPayload scope.
         * @member {string} scope
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.scope = "";

        /**
         * ChatPayload time.
         * @member {number} time
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChatPayload is_system.
         * @member {boolean} is_system
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.is_system = false;

        /**
         * ChatPayload message_id.
         * @member {string} message_id
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.message_id = "";

        /**
         * ChatPayload room_code.
         * @member {string} room_code
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.room_code = "";

        /**
         * ChatPayload game_id.
         * @member {string} game_id
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.game_id = "";

        /**
         * ChatPayload server_time.
         * @member {number} server_time
         * @memberof protocol.ChatPayload
         * @instance
         */
        ChatPayload.prototype.server_time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified ChatPayload message. Does not implicitly {@link protocol.ChatPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.ChatPayload
         * @static
         * @param {protocol.ChatPayload.$Properties} message ChatPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChatPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sender_id != null && $Object.hasOwnProperty.call(message, "sender_id") && message.sender_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.sender_id);
            if (message.sender_name != null && $Object.hasOwnProperty.call(message, "sender_name") && message.sender_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.sender_name);
            if (message.content != null && $Object.hasOwnProperty.call(message, "content") && message.content !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.content);
            if (message.scope != null && $Object.hasOwnProperty.call(message, "scope") && message.scope !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.scope);
            if (message.time != null && $Object.hasOwnProperty.call(message, "time") && (typeof message.time === "object" ? message.time.low || message.time.high : message.time !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.time);
            if (message.is_system != null && $Object.hasOwnProperty.call(message, "is_system") && message.is_system !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.is_system);
            if (message.message_id != null && $Object.hasOwnProperty.call(message, "message_id") && message.message_id !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.message_id);
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code") && message.room_code !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.room_code);
            if (message.game_id != null && $Object.hasOwnProperty.call(message, "game_id") && message.game_id !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.game_id);
            if (message.server_time != null && $Object.hasOwnProperty.call(message, "server_time") && (typeof message.server_time === "object" ? message.server_time.low || message.server_time.high : message.server_time !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.server_time);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a ChatPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.ChatPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.ChatPayload & protocol.ChatPayload.$Shape} ChatPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChatPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.ChatPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.sender_id = value;
                        else
                            delete message.sender_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.sender_name = value;
                        else
                            delete message.sender_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.scope = value;
                        else
                            delete message.scope;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_system = value;
                        else
                            delete message.is_system;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message_id = value;
                        else
                            delete message.message_id;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.room_code = value;
                        else
                            delete message.room_code;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.game_id = value;
                        else
                            delete message.game_id;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.server_time = value;
                        else
                            delete message.server_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a ChatPayload message.
         * @function verify
         * @memberof protocol.ChatPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChatPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sender_id != null && $Object.hasOwnProperty.call(message, "sender_id"))
                if (!$util.isString(message.sender_id))
                    return "sender_id: string expected";
            if (message.sender_name != null && $Object.hasOwnProperty.call(message, "sender_name"))
                if (!$util.isString(message.sender_name))
                    return "sender_name: string expected";
            if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.scope != null && $Object.hasOwnProperty.call(message, "scope"))
                if (!$util.isString(message.scope))
                    return "scope: string expected";
            if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                    return "time: integer|Long expected";
            if (message.is_system != null && $Object.hasOwnProperty.call(message, "is_system"))
                if (typeof message.is_system !== "boolean")
                    return "is_system: boolean expected";
            if (message.message_id != null && $Object.hasOwnProperty.call(message, "message_id"))
                if (!$util.isString(message.message_id))
                    return "message_id: string expected";
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code"))
                if (!$util.isString(message.room_code))
                    return "room_code: string expected";
            if (message.game_id != null && $Object.hasOwnProperty.call(message, "game_id"))
                if (!$util.isString(message.game_id))
                    return "game_id: string expected";
            if (message.server_time != null && $Object.hasOwnProperty.call(message, "server_time"))
                if (!$util.isInteger(message.server_time) && !(message.server_time && $util.isInteger(message.server_time.low) && $util.isInteger(message.server_time.high)))
                    return "server_time: integer|Long expected";
            return null;
        };

        return ChatPayload;
    })();

    protocol.LeaderboardEntry = (function() {

        /**
         * Properties of a LeaderboardEntry.
         * @typedef {Object} protocol.LeaderboardEntry.$Properties
         * @property {number|null} [rank] LeaderboardEntry rank
         * @property {string|null} [player_id] LeaderboardEntry player_id
         * @property {string|null} [player_name] LeaderboardEntry player_name
         * @property {number|null} [score] LeaderboardEntry score
         * @property {number|null} [wins] LeaderboardEntry wins
         * @property {number|null} [win_rate] LeaderboardEntry win_rate
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LeaderboardEntry.
         * @memberof protocol
         * @interface ILeaderboardEntry
         * @augments protocol.LeaderboardEntry.$Properties
         * @deprecated Use protocol.LeaderboardEntry.$Properties instead.
         */

        /**
         * Shape of a LeaderboardEntry.
         * @typedef {protocol.LeaderboardEntry.$Properties} protocol.LeaderboardEntry.$Shape
         */

        /**
         * Constructs a new LeaderboardEntry.
         * @memberof protocol
         * @classdesc Represents a LeaderboardEntry.
         * @constructor
         * @param {protocol.LeaderboardEntry.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LeaderboardEntry = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LeaderboardEntry rank.
         * @member {number} rank
         * @memberof protocol.LeaderboardEntry
         * @instance
         */
        LeaderboardEntry.prototype.rank = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LeaderboardEntry player_id.
         * @member {string} player_id
         * @memberof protocol.LeaderboardEntry
         * @instance
         */
        LeaderboardEntry.prototype.player_id = "";

        /**
         * LeaderboardEntry player_name.
         * @member {string} player_name
         * @memberof protocol.LeaderboardEntry
         * @instance
         */
        LeaderboardEntry.prototype.player_name = "";

        /**
         * LeaderboardEntry score.
         * @member {number} score
         * @memberof protocol.LeaderboardEntry
         * @instance
         */
        LeaderboardEntry.prototype.score = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LeaderboardEntry wins.
         * @member {number} wins
         * @memberof protocol.LeaderboardEntry
         * @instance
         */
        LeaderboardEntry.prototype.wins = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LeaderboardEntry win_rate.
         * @member {number} win_rate
         * @memberof protocol.LeaderboardEntry
         * @instance
         */
        LeaderboardEntry.prototype.win_rate = 0;

        /**
         * Encodes the specified LeaderboardEntry message. Does not implicitly {@link protocol.LeaderboardEntry.verify|verify} messages.
         * @function encode
         * @memberof protocol.LeaderboardEntry
         * @static
         * @param {protocol.LeaderboardEntry.$Properties} message LeaderboardEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LeaderboardEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.rank != null && $Object.hasOwnProperty.call(message, "rank") && (typeof message.rank === "object" ? message.rank.low || message.rank.high : message.rank !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.rank);
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.player_name);
            if (message.score != null && $Object.hasOwnProperty.call(message, "score") && (typeof message.score === "object" ? message.score.low || message.score.high : message.score !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.score);
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins") && (typeof message.wins === "object" ? message.wins.low || message.wins.high : message.wins !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.wins);
            if (message.win_rate != null && $Object.hasOwnProperty.call(message, "win_rate") && !$Object.is(message.win_rate, 0))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.win_rate);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a LeaderboardEntry message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.LeaderboardEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.LeaderboardEntry & protocol.LeaderboardEntry.$Shape} LeaderboardEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LeaderboardEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.LeaderboardEntry(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.rank = value;
                        else
                            delete message.rank;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.wins = value;
                        else
                            delete message.wins;
                        continue;
                    }
                case 6: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.win_rate = value;
                        else
                            delete message.win_rate;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a LeaderboardEntry message.
         * @function verify
         * @memberof protocol.LeaderboardEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LeaderboardEntry.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.rank != null && $Object.hasOwnProperty.call(message, "rank"))
                if (!$util.isInteger(message.rank) && !(message.rank && $util.isInteger(message.rank.low) && $util.isInteger(message.rank.high)))
                    return "rank: integer|Long expected";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
                if (!$util.isInteger(message.score) && !(message.score && $util.isInteger(message.score.low) && $util.isInteger(message.score.high)))
                    return "score: integer|Long expected";
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                if (!$util.isInteger(message.wins) && !(message.wins && $util.isInteger(message.wins.low) && $util.isInteger(message.wins.high)))
                    return "wins: integer|Long expected";
            if (message.win_rate != null && $Object.hasOwnProperty.call(message, "win_rate"))
                if (typeof message.win_rate !== "number")
                    return "win_rate: number expected";
            return null;
        };

        return LeaderboardEntry;
    })();

    protocol.RoomListItem = (function() {

        /**
         * Properties of a RoomListItem.
         * @typedef {Object} protocol.RoomListItem.$Properties
         * @property {string|null} [room_code] RoomListItem room_code
         * @property {number|null} [player_count] RoomListItem player_count
         * @property {number|null} [max_players] RoomListItem max_players
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomListItem.
         * @memberof protocol
         * @interface IRoomListItem
         * @augments protocol.RoomListItem.$Properties
         * @deprecated Use protocol.RoomListItem.$Properties instead.
         */

        /**
         * Shape of a RoomListItem.
         * @typedef {protocol.RoomListItem.$Properties} protocol.RoomListItem.$Shape
         */

        /**
         * Constructs a new RoomListItem.
         * @memberof protocol
         * @classdesc Represents a RoomListItem.
         * @constructor
         * @param {protocol.RoomListItem.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomListItem = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomListItem room_code.
         * @member {string} room_code
         * @memberof protocol.RoomListItem
         * @instance
         */
        RoomListItem.prototype.room_code = "";

        /**
         * RoomListItem player_count.
         * @member {number} player_count
         * @memberof protocol.RoomListItem
         * @instance
         */
        RoomListItem.prototype.player_count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoomListItem max_players.
         * @member {number} max_players
         * @memberof protocol.RoomListItem
         * @instance
         */
        RoomListItem.prototype.max_players = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified RoomListItem message. Does not implicitly {@link protocol.RoomListItem.verify|verify} messages.
         * @function encode
         * @memberof protocol.RoomListItem
         * @static
         * @param {protocol.RoomListItem.$Properties} message RoomListItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomListItem.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code") && message.room_code !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.room_code);
            if (message.player_count != null && $Object.hasOwnProperty.call(message, "player_count") && (typeof message.player_count === "object" ? message.player_count.low || message.player_count.high : message.player_count !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.player_count);
            if (message.max_players != null && $Object.hasOwnProperty.call(message, "max_players") && (typeof message.max_players === "object" ? message.max_players.low || message.max_players.high : message.max_players !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.max_players);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a RoomListItem message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.RoomListItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.RoomListItem & protocol.RoomListItem.$Shape} RoomListItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomListItem.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.RoomListItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.room_code = value;
                        else
                            delete message.room_code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.player_count = value;
                        else
                            delete message.player_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_players = value;
                        else
                            delete message.max_players;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a RoomListItem message.
         * @function verify
         * @memberof protocol.RoomListItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomListItem.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code"))
                if (!$util.isString(message.room_code))
                    return "room_code: string expected";
            if (message.player_count != null && $Object.hasOwnProperty.call(message, "player_count"))
                if (!$util.isInteger(message.player_count) && !(message.player_count && $util.isInteger(message.player_count.low) && $util.isInteger(message.player_count.high)))
                    return "player_count: integer|Long expected";
            if (message.max_players != null && $Object.hasOwnProperty.call(message, "max_players"))
                if (!$util.isInteger(message.max_players) && !(message.max_players && $util.isInteger(message.max_players.low) && $util.isInteger(message.max_players.high)))
                    return "max_players: integer|Long expected";
            return null;
        };

        return RoomListItem;
    })();

    protocol.HelloPayload = (function() {

        /**
         * Properties of a HelloPayload.
         * @typedef {Object} protocol.HelloPayload.$Properties
         * @property {string|null} [protocol_version] HelloPayload protocol_version
         * @property {string|null} [client_version] HelloPayload client_version
         * @property {Array.<string>|null} [capabilities] HelloPayload capabilities
         * @property {string|null} [client_kind] HelloPayload client_kind
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a HelloPayload.
         * @memberof protocol
         * @interface IHelloPayload
         * @augments protocol.HelloPayload.$Properties
         * @deprecated Use protocol.HelloPayload.$Properties instead.
         */

        /**
         * Shape of a HelloPayload.
         * @typedef {protocol.HelloPayload.$Properties} protocol.HelloPayload.$Shape
         */

        /**
         * Constructs a new HelloPayload.
         * @memberof protocol
         * @classdesc Represents a HelloPayload.
         * @constructor
         * @param {protocol.HelloPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const HelloPayload = function (properties) {
            this.capabilities = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HelloPayload protocol_version.
         * @member {string} protocol_version
         * @memberof protocol.HelloPayload
         * @instance
         */
        HelloPayload.prototype.protocol_version = "";

        /**
         * HelloPayload client_version.
         * @member {string} client_version
         * @memberof protocol.HelloPayload
         * @instance
         */
        HelloPayload.prototype.client_version = "";

        /**
         * HelloPayload capabilities.
         * @member {Array.<string>} capabilities
         * @memberof protocol.HelloPayload
         * @instance
         */
        HelloPayload.prototype.capabilities = $util.emptyArray;

        /**
         * HelloPayload client_kind.
         * @member {string} client_kind
         * @memberof protocol.HelloPayload
         * @instance
         */
        HelloPayload.prototype.client_kind = "";

        /**
         * Encodes the specified HelloPayload message. Does not implicitly {@link protocol.HelloPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.HelloPayload
         * @static
         * @param {protocol.HelloPayload.$Properties} message HelloPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HelloPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.protocol_version != null && $Object.hasOwnProperty.call(message, "protocol_version") && message.protocol_version !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.protocol_version);
            if (message.client_version != null && $Object.hasOwnProperty.call(message, "client_version") && message.client_version !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.client_version);
            if (message.capabilities != null && message.capabilities.length)
                for (let i = 0; i < message.capabilities.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.capabilities[i]);
            if (message.client_kind != null && $Object.hasOwnProperty.call(message, "client_kind") && message.client_kind !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.client_kind);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a HelloPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.HelloPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.HelloPayload & protocol.HelloPayload.$Shape} HelloPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HelloPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.HelloPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.protocol_version = value;
                        else
                            delete message.protocol_version;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.client_version = value;
                        else
                            delete message.client_version;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.capabilities && message.capabilities.length))
                            message.capabilities = [];
                        message.capabilities.push(reader.stringVerify());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.client_kind = value;
                        else
                            delete message.client_kind;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a HelloPayload message.
         * @function verify
         * @memberof protocol.HelloPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HelloPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.protocol_version != null && $Object.hasOwnProperty.call(message, "protocol_version"))
                if (!$util.isString(message.protocol_version))
                    return "protocol_version: string expected";
            if (message.client_version != null && $Object.hasOwnProperty.call(message, "client_version"))
                if (!$util.isString(message.client_version))
                    return "client_version: string expected";
            if (message.capabilities != null && $Object.hasOwnProperty.call(message, "capabilities")) {
                if (!$Array.isArray(message.capabilities))
                    return "capabilities: array expected";
                for (let i = 0; i < message.capabilities.length; ++i)
                    if (!$util.isString(message.capabilities[i]))
                        return "capabilities: string[] expected";
            }
            if (message.client_kind != null && $Object.hasOwnProperty.call(message, "client_kind"))
                if (!$util.isString(message.client_kind))
                    return "client_kind: string expected";
            return null;
        };

        return HelloPayload;
    })();

    protocol.ReconnectPayload = (function() {

        /**
         * Properties of a ReconnectPayload.
         * @typedef {Object} protocol.ReconnectPayload.$Properties
         * @property {string|null} [token] ReconnectPayload token
         * @property {string|null} [player_id] ReconnectPayload player_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ReconnectPayload.
         * @memberof protocol
         * @interface IReconnectPayload
         * @augments protocol.ReconnectPayload.$Properties
         * @deprecated Use protocol.ReconnectPayload.$Properties instead.
         */

        /**
         * Shape of a ReconnectPayload.
         * @typedef {protocol.ReconnectPayload.$Properties} protocol.ReconnectPayload.$Shape
         */

        /**
         * Constructs a new ReconnectPayload.
         * @memberof protocol
         * @classdesc Represents a ReconnectPayload.
         * @constructor
         * @param {protocol.ReconnectPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ReconnectPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ReconnectPayload token.
         * @member {string} token
         * @memberof protocol.ReconnectPayload
         * @instance
         */
        ReconnectPayload.prototype.token = "";

        /**
         * ReconnectPayload player_id.
         * @member {string} player_id
         * @memberof protocol.ReconnectPayload
         * @instance
         */
        ReconnectPayload.prototype.player_id = "";

        /**
         * Encodes the specified ReconnectPayload message. Does not implicitly {@link protocol.ReconnectPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.ReconnectPayload
         * @static
         * @param {protocol.ReconnectPayload.$Properties} message ReconnectPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReconnectPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token") && message.token !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.token);
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a ReconnectPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.ReconnectPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.ReconnectPayload & protocol.ReconnectPayload.$Shape} ReconnectPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReconnectPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.ReconnectPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a ReconnectPayload message.
         * @function verify
         * @memberof protocol.ReconnectPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReconnectPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            return null;
        };

        return ReconnectPayload;
    })();

    protocol.PingPayload = (function() {

        /**
         * Properties of a PingPayload.
         * @typedef {Object} protocol.PingPayload.$Properties
         * @property {number|null} [timestamp] PingPayload timestamp
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PingPayload.
         * @memberof protocol
         * @interface IPingPayload
         * @augments protocol.PingPayload.$Properties
         * @deprecated Use protocol.PingPayload.$Properties instead.
         */

        /**
         * Shape of a PingPayload.
         * @typedef {protocol.PingPayload.$Properties} protocol.PingPayload.$Shape
         */

        /**
         * Constructs a new PingPayload.
         * @memberof protocol
         * @classdesc Represents a PingPayload.
         * @constructor
         * @param {protocol.PingPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PingPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PingPayload timestamp.
         * @member {number} timestamp
         * @memberof protocol.PingPayload
         * @instance
         */
        PingPayload.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified PingPayload message. Does not implicitly {@link protocol.PingPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PingPayload
         * @static
         * @param {protocol.PingPayload.$Properties} message PingPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PingPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp") && (typeof message.timestamp === "object" ? message.timestamp.low || message.timestamp.high : message.timestamp !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.timestamp);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PingPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PingPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PingPayload & protocol.PingPayload.$Shape} PingPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PingPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PingPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PingPayload message.
         * @function verify
         * @memberof protocol.PingPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PingPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            return null;
        };

        return PingPayload;
    })();

    protocol.JoinRoomPayload = (function() {

        /**
         * Properties of a JoinRoomPayload.
         * @typedef {Object} protocol.JoinRoomPayload.$Properties
         * @property {string|null} [room_code] JoinRoomPayload room_code
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a JoinRoomPayload.
         * @memberof protocol
         * @interface IJoinRoomPayload
         * @augments protocol.JoinRoomPayload.$Properties
         * @deprecated Use protocol.JoinRoomPayload.$Properties instead.
         */

        /**
         * Shape of a JoinRoomPayload.
         * @typedef {protocol.JoinRoomPayload.$Properties} protocol.JoinRoomPayload.$Shape
         */

        /**
         * Constructs a new JoinRoomPayload.
         * @memberof protocol
         * @classdesc Represents a JoinRoomPayload.
         * @constructor
         * @param {protocol.JoinRoomPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const JoinRoomPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * JoinRoomPayload room_code.
         * @member {string} room_code
         * @memberof protocol.JoinRoomPayload
         * @instance
         */
        JoinRoomPayload.prototype.room_code = "";

        /**
         * Encodes the specified JoinRoomPayload message. Does not implicitly {@link protocol.JoinRoomPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.JoinRoomPayload
         * @static
         * @param {protocol.JoinRoomPayload.$Properties} message JoinRoomPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JoinRoomPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code") && message.room_code !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.room_code);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a JoinRoomPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.JoinRoomPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.JoinRoomPayload & protocol.JoinRoomPayload.$Shape} JoinRoomPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JoinRoomPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.JoinRoomPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.room_code = value;
                        else
                            delete message.room_code;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a JoinRoomPayload message.
         * @function verify
         * @memberof protocol.JoinRoomPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JoinRoomPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code"))
                if (!$util.isString(message.room_code))
                    return "room_code: string expected";
            return null;
        };

        return JoinRoomPayload;
    })();

    protocol.BidPayload = (function() {

        /**
         * Properties of a BidPayload.
         * @typedef {Object} protocol.BidPayload.$Properties
         * @property {boolean|null} [bid] BidPayload bid
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BidPayload.
         * @memberof protocol
         * @interface IBidPayload
         * @augments protocol.BidPayload.$Properties
         * @deprecated Use protocol.BidPayload.$Properties instead.
         */

        /**
         * Shape of a BidPayload.
         * @typedef {protocol.BidPayload.$Properties} protocol.BidPayload.$Shape
         */

        /**
         * Constructs a new BidPayload.
         * @memberof protocol
         * @classdesc Represents a BidPayload.
         * @constructor
         * @param {protocol.BidPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BidPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BidPayload bid.
         * @member {boolean} bid
         * @memberof protocol.BidPayload
         * @instance
         */
        BidPayload.prototype.bid = false;

        /**
         * Encodes the specified BidPayload message. Does not implicitly {@link protocol.BidPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.BidPayload
         * @static
         * @param {protocol.BidPayload.$Properties} message BidPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BidPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.bid != null && $Object.hasOwnProperty.call(message, "bid") && message.bid !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.bid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a BidPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.BidPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.BidPayload & protocol.BidPayload.$Shape} BidPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BidPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.BidPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.bid = value;
                        else
                            delete message.bid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a BidPayload message.
         * @function verify
         * @memberof protocol.BidPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BidPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.bid != null && $Object.hasOwnProperty.call(message, "bid"))
                if (typeof message.bid !== "boolean")
                    return "bid: boolean expected";
            return null;
        };

        return BidPayload;
    })();

    protocol.PlayCardsPayload = (function() {

        /**
         * Properties of a PlayCardsPayload.
         * @typedef {Object} protocol.PlayCardsPayload.$Properties
         * @property {Array.<protocol.CardInfo.$Properties>|null} [cards] PlayCardsPayload cards
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayCardsPayload.
         * @memberof protocol
         * @interface IPlayCardsPayload
         * @augments protocol.PlayCardsPayload.$Properties
         * @deprecated Use protocol.PlayCardsPayload.$Properties instead.
         */

        /**
         * Shape of a PlayCardsPayload.
         * @typedef {protocol.PlayCardsPayload.$Properties} protocol.PlayCardsPayload.$Shape
         */

        /**
         * Constructs a new PlayCardsPayload.
         * @memberof protocol
         * @classdesc Represents a PlayCardsPayload.
         * @constructor
         * @param {protocol.PlayCardsPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayCardsPayload = function (properties) {
            this.cards = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayCardsPayload cards.
         * @member {Array.<protocol.CardInfo.$Properties>} cards
         * @memberof protocol.PlayCardsPayload
         * @instance
         */
        PlayCardsPayload.prototype.cards = $util.emptyArray;

        /**
         * Encodes the specified PlayCardsPayload message. Does not implicitly {@link protocol.PlayCardsPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayCardsPayload
         * @static
         * @param {protocol.PlayCardsPayload.$Properties} message PlayCardsPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayCardsPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.cards != null && message.cards.length)
                for (let i = 0; i < message.cards.length; ++i)
                    $root.protocol.CardInfo.encode(message.cards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayCardsPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayCardsPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayCardsPayload & protocol.PlayCardsPayload.$Shape} PlayCardsPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayCardsPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayCardsPayload();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayCardsPayload message.
         * @function verify
         * @memberof protocol.PlayCardsPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayCardsPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.cards != null && $Object.hasOwnProperty.call(message, "cards")) {
                if (!$Array.isArray(message.cards))
                    return "cards: array expected";
                for (let i = 0; i < message.cards.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.cards[i], _depth + 1);
                    if (error)
                        return "cards." + error;
                }
            }
            return null;
        };

        return PlayCardsPayload;
    })();

    protocol.GetLeaderboardPayload = (function() {

        /**
         * Properties of a GetLeaderboardPayload.
         * @typedef {Object} protocol.GetLeaderboardPayload.$Properties
         * @property {string|null} [type] GetLeaderboardPayload type
         * @property {number|null} [offset] GetLeaderboardPayload offset
         * @property {number|null} [limit] GetLeaderboardPayload limit
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GetLeaderboardPayload.
         * @memberof protocol
         * @interface IGetLeaderboardPayload
         * @augments protocol.GetLeaderboardPayload.$Properties
         * @deprecated Use protocol.GetLeaderboardPayload.$Properties instead.
         */

        /**
         * Shape of a GetLeaderboardPayload.
         * @typedef {protocol.GetLeaderboardPayload.$Properties} protocol.GetLeaderboardPayload.$Shape
         */

        /**
         * Constructs a new GetLeaderboardPayload.
         * @memberof protocol
         * @classdesc Represents a GetLeaderboardPayload.
         * @constructor
         * @param {protocol.GetLeaderboardPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GetLeaderboardPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GetLeaderboardPayload type.
         * @member {string} type
         * @memberof protocol.GetLeaderboardPayload
         * @instance
         */
        GetLeaderboardPayload.prototype.type = "";

        /**
         * GetLeaderboardPayload offset.
         * @member {number} offset
         * @memberof protocol.GetLeaderboardPayload
         * @instance
         */
        GetLeaderboardPayload.prototype.offset = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetLeaderboardPayload limit.
         * @member {number} limit
         * @memberof protocol.GetLeaderboardPayload
         * @instance
         */
        GetLeaderboardPayload.prototype.limit = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified GetLeaderboardPayload message. Does not implicitly {@link protocol.GetLeaderboardPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.GetLeaderboardPayload
         * @static
         * @param {protocol.GetLeaderboardPayload.$Properties} message GetLeaderboardPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetLeaderboardPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.type);
            if (message.offset != null && $Object.hasOwnProperty.call(message, "offset") && (typeof message.offset === "object" ? message.offset.low || message.offset.high : message.offset !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.offset);
            if (message.limit != null && $Object.hasOwnProperty.call(message, "limit") && (typeof message.limit === "object" ? message.limit.low || message.limit.high : message.limit !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.limit);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a GetLeaderboardPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.GetLeaderboardPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.GetLeaderboardPayload & protocol.GetLeaderboardPayload.$Shape} GetLeaderboardPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetLeaderboardPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.GetLeaderboardPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.offset = value;
                        else
                            delete message.offset;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.limit = value;
                        else
                            delete message.limit;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a GetLeaderboardPayload message.
         * @function verify
         * @memberof protocol.GetLeaderboardPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetLeaderboardPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.offset != null && $Object.hasOwnProperty.call(message, "offset"))
                if (!$util.isInteger(message.offset) && !(message.offset && $util.isInteger(message.offset.low) && $util.isInteger(message.offset.high)))
                    return "offset: integer|Long expected";
            if (message.limit != null && $Object.hasOwnProperty.call(message, "limit"))
                if (!$util.isInteger(message.limit) && !(message.limit && $util.isInteger(message.limit.low) && $util.isInteger(message.limit.high)))
                    return "limit: integer|Long expected";
            return null;
        };

        return GetLeaderboardPayload;
    })();

    protocol.NegotiatedPayload = (function() {

        /**
         * Properties of a NegotiatedPayload.
         * @typedef {Object} protocol.NegotiatedPayload.$Properties
         * @property {string|null} [protocol_version] NegotiatedPayload protocol_version
         * @property {string|null} [server_version] NegotiatedPayload server_version
         * @property {Array.<string>|null} [capabilities] NegotiatedPayload capabilities
         * @property {string|null} [client_kind] NegotiatedPayload client_kind
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NegotiatedPayload.
         * @memberof protocol
         * @interface INegotiatedPayload
         * @augments protocol.NegotiatedPayload.$Properties
         * @deprecated Use protocol.NegotiatedPayload.$Properties instead.
         */

        /**
         * Shape of a NegotiatedPayload.
         * @typedef {protocol.NegotiatedPayload.$Properties} protocol.NegotiatedPayload.$Shape
         */

        /**
         * Constructs a new NegotiatedPayload.
         * @memberof protocol
         * @classdesc Represents a NegotiatedPayload.
         * @constructor
         * @param {protocol.NegotiatedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NegotiatedPayload = function (properties) {
            this.capabilities = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NegotiatedPayload protocol_version.
         * @member {string} protocol_version
         * @memberof protocol.NegotiatedPayload
         * @instance
         */
        NegotiatedPayload.prototype.protocol_version = "";

        /**
         * NegotiatedPayload server_version.
         * @member {string} server_version
         * @memberof protocol.NegotiatedPayload
         * @instance
         */
        NegotiatedPayload.prototype.server_version = "";

        /**
         * NegotiatedPayload capabilities.
         * @member {Array.<string>} capabilities
         * @memberof protocol.NegotiatedPayload
         * @instance
         */
        NegotiatedPayload.prototype.capabilities = $util.emptyArray;

        /**
         * NegotiatedPayload client_kind.
         * @member {string} client_kind
         * @memberof protocol.NegotiatedPayload
         * @instance
         */
        NegotiatedPayload.prototype.client_kind = "";

        /**
         * Encodes the specified NegotiatedPayload message. Does not implicitly {@link protocol.NegotiatedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.NegotiatedPayload
         * @static
         * @param {protocol.NegotiatedPayload.$Properties} message NegotiatedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NegotiatedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.protocol_version != null && $Object.hasOwnProperty.call(message, "protocol_version") && message.protocol_version !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.protocol_version);
            if (message.server_version != null && $Object.hasOwnProperty.call(message, "server_version") && message.server_version !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.server_version);
            if (message.capabilities != null && message.capabilities.length)
                for (let i = 0; i < message.capabilities.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.capabilities[i]);
            if (message.client_kind != null && $Object.hasOwnProperty.call(message, "client_kind") && message.client_kind !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.client_kind);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a NegotiatedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.NegotiatedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.NegotiatedPayload & protocol.NegotiatedPayload.$Shape} NegotiatedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NegotiatedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.NegotiatedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.protocol_version = value;
                        else
                            delete message.protocol_version;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.server_version = value;
                        else
                            delete message.server_version;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.capabilities && message.capabilities.length))
                            message.capabilities = [];
                        message.capabilities.push(reader.stringVerify());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.client_kind = value;
                        else
                            delete message.client_kind;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a NegotiatedPayload message.
         * @function verify
         * @memberof protocol.NegotiatedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NegotiatedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.protocol_version != null && $Object.hasOwnProperty.call(message, "protocol_version"))
                if (!$util.isString(message.protocol_version))
                    return "protocol_version: string expected";
            if (message.server_version != null && $Object.hasOwnProperty.call(message, "server_version"))
                if (!$util.isString(message.server_version))
                    return "server_version: string expected";
            if (message.capabilities != null && $Object.hasOwnProperty.call(message, "capabilities")) {
                if (!$Array.isArray(message.capabilities))
                    return "capabilities: array expected";
                for (let i = 0; i < message.capabilities.length; ++i)
                    if (!$util.isString(message.capabilities[i]))
                        return "capabilities: string[] expected";
            }
            if (message.client_kind != null && $Object.hasOwnProperty.call(message, "client_kind"))
                if (!$util.isString(message.client_kind))
                    return "client_kind: string expected";
            return null;
        };

        return NegotiatedPayload;
    })();

    protocol.ProtocolRejectedPayload = (function() {

        /**
         * Properties of a ProtocolRejectedPayload.
         * @typedef {Object} protocol.ProtocolRejectedPayload.$Properties
         * @property {string|null} [request_id] ProtocolRejectedPayload request_id
         * @property {string|null} [reason] ProtocolRejectedPayload reason
         * @property {string|null} [supported_protocol_version] ProtocolRejectedPayload supported_protocol_version
         * @property {string|null} [min_client_version] ProtocolRejectedPayload min_client_version
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ProtocolRejectedPayload.
         * @memberof protocol
         * @interface IProtocolRejectedPayload
         * @augments protocol.ProtocolRejectedPayload.$Properties
         * @deprecated Use protocol.ProtocolRejectedPayload.$Properties instead.
         */

        /**
         * Shape of a ProtocolRejectedPayload.
         * @typedef {protocol.ProtocolRejectedPayload.$Properties} protocol.ProtocolRejectedPayload.$Shape
         */

        /**
         * Constructs a new ProtocolRejectedPayload.
         * @memberof protocol
         * @classdesc Represents a ProtocolRejectedPayload.
         * @constructor
         * @param {protocol.ProtocolRejectedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ProtocolRejectedPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ProtocolRejectedPayload request_id.
         * @member {string} request_id
         * @memberof protocol.ProtocolRejectedPayload
         * @instance
         */
        ProtocolRejectedPayload.prototype.request_id = "";

        /**
         * ProtocolRejectedPayload reason.
         * @member {string} reason
         * @memberof protocol.ProtocolRejectedPayload
         * @instance
         */
        ProtocolRejectedPayload.prototype.reason = "";

        /**
         * ProtocolRejectedPayload supported_protocol_version.
         * @member {string} supported_protocol_version
         * @memberof protocol.ProtocolRejectedPayload
         * @instance
         */
        ProtocolRejectedPayload.prototype.supported_protocol_version = "";

        /**
         * ProtocolRejectedPayload min_client_version.
         * @member {string} min_client_version
         * @memberof protocol.ProtocolRejectedPayload
         * @instance
         */
        ProtocolRejectedPayload.prototype.min_client_version = "";

        /**
         * Encodes the specified ProtocolRejectedPayload message. Does not implicitly {@link protocol.ProtocolRejectedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.ProtocolRejectedPayload
         * @static
         * @param {protocol.ProtocolRejectedPayload.$Properties} message ProtocolRejectedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtocolRejectedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id") && message.request_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.request_id);
            if (message.reason != null && $Object.hasOwnProperty.call(message, "reason") && message.reason !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.reason);
            if (message.supported_protocol_version != null && $Object.hasOwnProperty.call(message, "supported_protocol_version") && message.supported_protocol_version !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.supported_protocol_version);
            if (message.min_client_version != null && $Object.hasOwnProperty.call(message, "min_client_version") && message.min_client_version !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.min_client_version);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a ProtocolRejectedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.ProtocolRejectedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.ProtocolRejectedPayload & protocol.ProtocolRejectedPayload.$Shape} ProtocolRejectedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtocolRejectedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.ProtocolRejectedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.request_id = value;
                        else
                            delete message.request_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.reason = value;
                        else
                            delete message.reason;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.supported_protocol_version = value;
                        else
                            delete message.supported_protocol_version;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.min_client_version = value;
                        else
                            delete message.min_client_version;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a ProtocolRejectedPayload message.
         * @function verify
         * @memberof protocol.ProtocolRejectedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProtocolRejectedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                if (!$util.isString(message.request_id))
                    return "request_id: string expected";
            if (message.reason != null && $Object.hasOwnProperty.call(message, "reason"))
                if (!$util.isString(message.reason))
                    return "reason: string expected";
            if (message.supported_protocol_version != null && $Object.hasOwnProperty.call(message, "supported_protocol_version"))
                if (!$util.isString(message.supported_protocol_version))
                    return "supported_protocol_version: string expected";
            if (message.min_client_version != null && $Object.hasOwnProperty.call(message, "min_client_version"))
                if (!$util.isString(message.min_client_version))
                    return "min_client_version: string expected";
            return null;
        };

        return ProtocolRejectedPayload;
    })();

    protocol.CommandAckPayload = (function() {

        /**
         * Properties of a CommandAckPayload.
         * @typedef {Object} protocol.CommandAckPayload.$Properties
         * @property {string|null} [request_id] CommandAckPayload request_id
         * @property {protocol.MessageType|null} [command_type] CommandAckPayload command_type
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CommandAckPayload.
         * @memberof protocol
         * @interface ICommandAckPayload
         * @augments protocol.CommandAckPayload.$Properties
         * @deprecated Use protocol.CommandAckPayload.$Properties instead.
         */

        /**
         * Shape of a CommandAckPayload.
         * @typedef {protocol.CommandAckPayload.$Properties} protocol.CommandAckPayload.$Shape
         */

        /**
         * Constructs a new CommandAckPayload.
         * @memberof protocol
         * @classdesc Represents a CommandAckPayload.
         * @constructor
         * @param {protocol.CommandAckPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CommandAckPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CommandAckPayload request_id.
         * @member {string} request_id
         * @memberof protocol.CommandAckPayload
         * @instance
         */
        CommandAckPayload.prototype.request_id = "";

        /**
         * CommandAckPayload command_type.
         * @member {protocol.MessageType} command_type
         * @memberof protocol.CommandAckPayload
         * @instance
         */
        CommandAckPayload.prototype.command_type = 0;

        /**
         * Encodes the specified CommandAckPayload message. Does not implicitly {@link protocol.CommandAckPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.CommandAckPayload
         * @static
         * @param {protocol.CommandAckPayload.$Properties} message CommandAckPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommandAckPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id") && message.request_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.request_id);
            if (message.command_type != null && $Object.hasOwnProperty.call(message, "command_type") && message.command_type !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.command_type);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a CommandAckPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.CommandAckPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.CommandAckPayload & protocol.CommandAckPayload.$Shape} CommandAckPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommandAckPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.CommandAckPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.request_id = value;
                        else
                            delete message.request_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.command_type = value;
                        else
                            delete message.command_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a CommandAckPayload message.
         * @function verify
         * @memberof protocol.CommandAckPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CommandAckPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                if (!$util.isString(message.request_id))
                    return "request_id: string expected";
            if (message.command_type != null && $Object.hasOwnProperty.call(message, "command_type"))
                if (typeof message.command_type !== "number" || (message.command_type | 0) !== message.command_type)
                    return "command_type: enum value expected";
            return null;
        };

        return CommandAckPayload;
    })();

    protocol.WarningPayload = (function() {

        /**
         * Properties of a WarningPayload.
         * @typedef {Object} protocol.WarningPayload.$Properties
         * @property {number|null} [code] WarningPayload code
         * @property {string|null} [message] WarningPayload message
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a WarningPayload.
         * @memberof protocol
         * @interface IWarningPayload
         * @augments protocol.WarningPayload.$Properties
         * @deprecated Use protocol.WarningPayload.$Properties instead.
         */

        /**
         * Shape of a WarningPayload.
         * @typedef {protocol.WarningPayload.$Properties} protocol.WarningPayload.$Shape
         */

        /**
         * Constructs a new WarningPayload.
         * @memberof protocol
         * @classdesc Represents a WarningPayload.
         * @constructor
         * @param {protocol.WarningPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const WarningPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * WarningPayload code.
         * @member {number} code
         * @memberof protocol.WarningPayload
         * @instance
         */
        WarningPayload.prototype.code = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * WarningPayload message.
         * @member {string} message
         * @memberof protocol.WarningPayload
         * @instance
         */
        WarningPayload.prototype.message = "";

        /**
         * Encodes the specified WarningPayload message. Does not implicitly {@link protocol.WarningPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.WarningPayload
         * @static
         * @param {protocol.WarningPayload.$Properties} message WarningPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WarningPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.code != null && $Object.hasOwnProperty.call(message, "code") && (typeof message.code === "object" ? message.code.low || message.code.high : message.code !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.code);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a WarningPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.WarningPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.WarningPayload & protocol.WarningPayload.$Shape} WarningPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WarningPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.WarningPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a WarningPayload message.
         * @function verify
         * @memberof protocol.WarningPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WarningPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                if (!$util.isInteger(message.code) && !(message.code && $util.isInteger(message.code.low) && $util.isInteger(message.code.high)))
                    return "code: integer|Long expected";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        return WarningPayload;
    })();

    protocol.ConnectedPayload = (function() {

        /**
         * Properties of a ConnectedPayload.
         * @typedef {Object} protocol.ConnectedPayload.$Properties
         * @property {string|null} [player_id] ConnectedPayload player_id
         * @property {string|null} [player_name] ConnectedPayload player_name
         * @property {string|null} [reconnect_token] ConnectedPayload reconnect_token
         * @property {string|null} [web_session_ticket] ConnectedPayload web_session_ticket
         * @property {boolean|null} [reconnect_available] ConnectedPayload reconnect_available
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConnectedPayload.
         * @memberof protocol
         * @interface IConnectedPayload
         * @augments protocol.ConnectedPayload.$Properties
         * @deprecated Use protocol.ConnectedPayload.$Properties instead.
         */

        /**
         * Shape of a ConnectedPayload.
         * @typedef {protocol.ConnectedPayload.$Properties} protocol.ConnectedPayload.$Shape
         */

        /**
         * Constructs a new ConnectedPayload.
         * @memberof protocol
         * @classdesc Represents a ConnectedPayload.
         * @constructor
         * @param {protocol.ConnectedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConnectedPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ConnectedPayload player_id.
         * @member {string} player_id
         * @memberof protocol.ConnectedPayload
         * @instance
         */
        ConnectedPayload.prototype.player_id = "";

        /**
         * ConnectedPayload player_name.
         * @member {string} player_name
         * @memberof protocol.ConnectedPayload
         * @instance
         */
        ConnectedPayload.prototype.player_name = "";

        /**
         * ConnectedPayload reconnect_token.
         * @member {string} reconnect_token
         * @memberof protocol.ConnectedPayload
         * @instance
         */
        ConnectedPayload.prototype.reconnect_token = "";

        /**
         * ConnectedPayload web_session_ticket.
         * @member {string} web_session_ticket
         * @memberof protocol.ConnectedPayload
         * @instance
         */
        ConnectedPayload.prototype.web_session_ticket = "";

        /**
         * ConnectedPayload reconnect_available.
         * @member {boolean} reconnect_available
         * @memberof protocol.ConnectedPayload
         * @instance
         */
        ConnectedPayload.prototype.reconnect_available = false;

        /**
         * Encodes the specified ConnectedPayload message. Does not implicitly {@link protocol.ConnectedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.ConnectedPayload
         * @static
         * @param {protocol.ConnectedPayload.$Properties} message ConnectedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConnectedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.reconnect_token != null && $Object.hasOwnProperty.call(message, "reconnect_token") && message.reconnect_token !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.reconnect_token);
            if (message.web_session_ticket != null && $Object.hasOwnProperty.call(message, "web_session_ticket") && message.web_session_ticket !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.web_session_ticket);
            if (message.reconnect_available != null && $Object.hasOwnProperty.call(message, "reconnect_available") && message.reconnect_available !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.reconnect_available);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a ConnectedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.ConnectedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.ConnectedPayload & protocol.ConnectedPayload.$Shape} ConnectedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConnectedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.ConnectedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.reconnect_token = value;
                        else
                            delete message.reconnect_token;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.web_session_ticket = value;
                        else
                            delete message.web_session_ticket;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.reconnect_available = value;
                        else
                            delete message.reconnect_available;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a ConnectedPayload message.
         * @function verify
         * @memberof protocol.ConnectedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConnectedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.reconnect_token != null && $Object.hasOwnProperty.call(message, "reconnect_token"))
                if (!$util.isString(message.reconnect_token))
                    return "reconnect_token: string expected";
            if (message.web_session_ticket != null && $Object.hasOwnProperty.call(message, "web_session_ticket"))
                if (!$util.isString(message.web_session_ticket))
                    return "web_session_ticket: string expected";
            if (message.reconnect_available != null && $Object.hasOwnProperty.call(message, "reconnect_available"))
                if (typeof message.reconnect_available !== "boolean")
                    return "reconnect_available: boolean expected";
            return null;
        };

        return ConnectedPayload;
    })();

    protocol.ReconnectedPayload = (function() {

        /**
         * Properties of a ReconnectedPayload.
         * @typedef {Object} protocol.ReconnectedPayload.$Properties
         * @property {string|null} [player_id] ReconnectedPayload player_id
         * @property {string|null} [player_name] ReconnectedPayload player_name
         * @property {string|null} [room_code] ReconnectedPayload room_code
         * @property {protocol.GameStateDTO.$Properties|null} [game_state] ReconnectedPayload game_state
         * @property {string|null} [reconnect_token] ReconnectedPayload reconnect_token
         * @property {string|null} [web_session_ticket] ReconnectedPayload web_session_ticket
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ReconnectedPayload.
         * @memberof protocol
         * @interface IReconnectedPayload
         * @augments protocol.ReconnectedPayload.$Properties
         * @deprecated Use protocol.ReconnectedPayload.$Properties instead.
         */

        /**
         * Shape of a ReconnectedPayload.
         * @typedef {protocol.ReconnectedPayload.$Properties} protocol.ReconnectedPayload.$Shape
         */

        /**
         * Constructs a new ReconnectedPayload.
         * @memberof protocol
         * @classdesc Represents a ReconnectedPayload.
         * @constructor
         * @param {protocol.ReconnectedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ReconnectedPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ReconnectedPayload player_id.
         * @member {string} player_id
         * @memberof protocol.ReconnectedPayload
         * @instance
         */
        ReconnectedPayload.prototype.player_id = "";

        /**
         * ReconnectedPayload player_name.
         * @member {string} player_name
         * @memberof protocol.ReconnectedPayload
         * @instance
         */
        ReconnectedPayload.prototype.player_name = "";

        /**
         * ReconnectedPayload room_code.
         * @member {string} room_code
         * @memberof protocol.ReconnectedPayload
         * @instance
         */
        ReconnectedPayload.prototype.room_code = "";

        /**
         * ReconnectedPayload game_state.
         * @member {protocol.GameStateDTO.$Properties|null|undefined} game_state
         * @memberof protocol.ReconnectedPayload
         * @instance
         */
        ReconnectedPayload.prototype.game_state = null;

        /**
         * ReconnectedPayload reconnect_token.
         * @member {string} reconnect_token
         * @memberof protocol.ReconnectedPayload
         * @instance
         */
        ReconnectedPayload.prototype.reconnect_token = "";

        /**
         * ReconnectedPayload web_session_ticket.
         * @member {string} web_session_ticket
         * @memberof protocol.ReconnectedPayload
         * @instance
         */
        ReconnectedPayload.prototype.web_session_ticket = "";

        /**
         * Encodes the specified ReconnectedPayload message. Does not implicitly {@link protocol.ReconnectedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.ReconnectedPayload
         * @static
         * @param {protocol.ReconnectedPayload.$Properties} message ReconnectedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReconnectedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code") && message.room_code !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.room_code);
            if (message.game_state != null && $Object.hasOwnProperty.call(message, "game_state"))
                $root.protocol.GameStateDTO.encode(message.game_state, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.reconnect_token != null && $Object.hasOwnProperty.call(message, "reconnect_token") && message.reconnect_token !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.reconnect_token);
            if (message.web_session_ticket != null && $Object.hasOwnProperty.call(message, "web_session_ticket") && message.web_session_ticket !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.web_session_ticket);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a ReconnectedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.ReconnectedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.ReconnectedPayload & protocol.ReconnectedPayload.$Shape} ReconnectedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReconnectedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.ReconnectedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.room_code = value;
                        else
                            delete message.room_code;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.game_state = $root.protocol.GameStateDTO.decode(reader, reader.uint32(), $undefined, _depth + 1, message.game_state);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.reconnect_token = value;
                        else
                            delete message.reconnect_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.web_session_ticket = value;
                        else
                            delete message.web_session_ticket;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a ReconnectedPayload message.
         * @function verify
         * @memberof protocol.ReconnectedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReconnectedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code"))
                if (!$util.isString(message.room_code))
                    return "room_code: string expected";
            if (message.game_state != null && $Object.hasOwnProperty.call(message, "game_state")) {
                let error = $root.protocol.GameStateDTO.verify(message.game_state, _depth + 1);
                if (error)
                    return "game_state." + error;
            }
            if (message.reconnect_token != null && $Object.hasOwnProperty.call(message, "reconnect_token"))
                if (!$util.isString(message.reconnect_token))
                    return "reconnect_token: string expected";
            if (message.web_session_ticket != null && $Object.hasOwnProperty.call(message, "web_session_ticket"))
                if (!$util.isString(message.web_session_ticket))
                    return "web_session_ticket: string expected";
            return null;
        };

        return ReconnectedPayload;
    })();

    protocol.PongPayload = (function() {

        /**
         * Properties of a PongPayload.
         * @typedef {Object} protocol.PongPayload.$Properties
         * @property {number|null} [client_timestamp] PongPayload client_timestamp
         * @property {number|null} [server_timestamp] PongPayload server_timestamp
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PongPayload.
         * @memberof protocol
         * @interface IPongPayload
         * @augments protocol.PongPayload.$Properties
         * @deprecated Use protocol.PongPayload.$Properties instead.
         */

        /**
         * Shape of a PongPayload.
         * @typedef {protocol.PongPayload.$Properties} protocol.PongPayload.$Shape
         */

        /**
         * Constructs a new PongPayload.
         * @memberof protocol
         * @classdesc Represents a PongPayload.
         * @constructor
         * @param {protocol.PongPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PongPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PongPayload client_timestamp.
         * @member {number} client_timestamp
         * @memberof protocol.PongPayload
         * @instance
         */
        PongPayload.prototype.client_timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PongPayload server_timestamp.
         * @member {number} server_timestamp
         * @memberof protocol.PongPayload
         * @instance
         */
        PongPayload.prototype.server_timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified PongPayload message. Does not implicitly {@link protocol.PongPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PongPayload
         * @static
         * @param {protocol.PongPayload.$Properties} message PongPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PongPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.client_timestamp != null && $Object.hasOwnProperty.call(message, "client_timestamp") && (typeof message.client_timestamp === "object" ? message.client_timestamp.low || message.client_timestamp.high : message.client_timestamp !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.client_timestamp);
            if (message.server_timestamp != null && $Object.hasOwnProperty.call(message, "server_timestamp") && (typeof message.server_timestamp === "object" ? message.server_timestamp.low || message.server_timestamp.high : message.server_timestamp !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.server_timestamp);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PongPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PongPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PongPayload & protocol.PongPayload.$Shape} PongPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PongPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PongPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.client_timestamp = value;
                        else
                            delete message.client_timestamp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.server_timestamp = value;
                        else
                            delete message.server_timestamp;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PongPayload message.
         * @function verify
         * @memberof protocol.PongPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PongPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.client_timestamp != null && $Object.hasOwnProperty.call(message, "client_timestamp"))
                if (!$util.isInteger(message.client_timestamp) && !(message.client_timestamp && $util.isInteger(message.client_timestamp.low) && $util.isInteger(message.client_timestamp.high)))
                    return "client_timestamp: integer|Long expected";
            if (message.server_timestamp != null && $Object.hasOwnProperty.call(message, "server_timestamp"))
                if (!$util.isInteger(message.server_timestamp) && !(message.server_timestamp && $util.isInteger(message.server_timestamp.low) && $util.isInteger(message.server_timestamp.high)))
                    return "server_timestamp: integer|Long expected";
            return null;
        };

        return PongPayload;
    })();

    protocol.PlayerOfflinePayload = (function() {

        /**
         * Properties of a PlayerOfflinePayload.
         * @typedef {Object} protocol.PlayerOfflinePayload.$Properties
         * @property {string|null} [player_id] PlayerOfflinePayload player_id
         * @property {string|null} [player_name] PlayerOfflinePayload player_name
         * @property {number|null} [timeout] PlayerOfflinePayload timeout
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerOfflinePayload.
         * @memberof protocol
         * @interface IPlayerOfflinePayload
         * @augments protocol.PlayerOfflinePayload.$Properties
         * @deprecated Use protocol.PlayerOfflinePayload.$Properties instead.
         */

        /**
         * Shape of a PlayerOfflinePayload.
         * @typedef {protocol.PlayerOfflinePayload.$Properties} protocol.PlayerOfflinePayload.$Shape
         */

        /**
         * Constructs a new PlayerOfflinePayload.
         * @memberof protocol
         * @classdesc Represents a PlayerOfflinePayload.
         * @constructor
         * @param {protocol.PlayerOfflinePayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerOfflinePayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerOfflinePayload player_id.
         * @member {string} player_id
         * @memberof protocol.PlayerOfflinePayload
         * @instance
         */
        PlayerOfflinePayload.prototype.player_id = "";

        /**
         * PlayerOfflinePayload player_name.
         * @member {string} player_name
         * @memberof protocol.PlayerOfflinePayload
         * @instance
         */
        PlayerOfflinePayload.prototype.player_name = "";

        /**
         * PlayerOfflinePayload timeout.
         * @member {number} timeout
         * @memberof protocol.PlayerOfflinePayload
         * @instance
         */
        PlayerOfflinePayload.prototype.timeout = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified PlayerOfflinePayload message. Does not implicitly {@link protocol.PlayerOfflinePayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerOfflinePayload
         * @static
         * @param {protocol.PlayerOfflinePayload.$Properties} message PlayerOfflinePayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerOfflinePayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout") && (typeof message.timeout === "object" ? message.timeout.low || message.timeout.high : message.timeout !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.timeout);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerOfflinePayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerOfflinePayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerOfflinePayload & protocol.PlayerOfflinePayload.$Shape} PlayerOfflinePayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerOfflinePayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerOfflinePayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.timeout = value;
                        else
                            delete message.timeout;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerOfflinePayload message.
         * @function verify
         * @memberof protocol.PlayerOfflinePayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerOfflinePayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                if (!$util.isInteger(message.timeout) && !(message.timeout && $util.isInteger(message.timeout.low) && $util.isInteger(message.timeout.high)))
                    return "timeout: integer|Long expected";
            return null;
        };

        return PlayerOfflinePayload;
    })();

    protocol.PlayerOnlinePayload = (function() {

        /**
         * Properties of a PlayerOnlinePayload.
         * @typedef {Object} protocol.PlayerOnlinePayload.$Properties
         * @property {string|null} [player_id] PlayerOnlinePayload player_id
         * @property {string|null} [player_name] PlayerOnlinePayload player_name
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerOnlinePayload.
         * @memberof protocol
         * @interface IPlayerOnlinePayload
         * @augments protocol.PlayerOnlinePayload.$Properties
         * @deprecated Use protocol.PlayerOnlinePayload.$Properties instead.
         */

        /**
         * Shape of a PlayerOnlinePayload.
         * @typedef {protocol.PlayerOnlinePayload.$Properties} protocol.PlayerOnlinePayload.$Shape
         */

        /**
         * Constructs a new PlayerOnlinePayload.
         * @memberof protocol
         * @classdesc Represents a PlayerOnlinePayload.
         * @constructor
         * @param {protocol.PlayerOnlinePayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerOnlinePayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerOnlinePayload player_id.
         * @member {string} player_id
         * @memberof protocol.PlayerOnlinePayload
         * @instance
         */
        PlayerOnlinePayload.prototype.player_id = "";

        /**
         * PlayerOnlinePayload player_name.
         * @member {string} player_name
         * @memberof protocol.PlayerOnlinePayload
         * @instance
         */
        PlayerOnlinePayload.prototype.player_name = "";

        /**
         * Encodes the specified PlayerOnlinePayload message. Does not implicitly {@link protocol.PlayerOnlinePayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerOnlinePayload
         * @static
         * @param {protocol.PlayerOnlinePayload.$Properties} message PlayerOnlinePayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerOnlinePayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerOnlinePayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerOnlinePayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerOnlinePayload & protocol.PlayerOnlinePayload.$Shape} PlayerOnlinePayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerOnlinePayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerOnlinePayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerOnlinePayload message.
         * @function verify
         * @memberof protocol.PlayerOnlinePayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerOnlinePayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            return null;
        };

        return PlayerOnlinePayload;
    })();

    protocol.OnlineCountPayload = (function() {

        /**
         * Properties of an OnlineCountPayload.
         * @typedef {Object} protocol.OnlineCountPayload.$Properties
         * @property {number|null} [count] OnlineCountPayload count
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an OnlineCountPayload.
         * @memberof protocol
         * @interface IOnlineCountPayload
         * @augments protocol.OnlineCountPayload.$Properties
         * @deprecated Use protocol.OnlineCountPayload.$Properties instead.
         */

        /**
         * Shape of an OnlineCountPayload.
         * @typedef {protocol.OnlineCountPayload.$Properties} protocol.OnlineCountPayload.$Shape
         */

        /**
         * Constructs a new OnlineCountPayload.
         * @memberof protocol
         * @classdesc Represents an OnlineCountPayload.
         * @constructor
         * @param {protocol.OnlineCountPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const OnlineCountPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OnlineCountPayload count.
         * @member {number} count
         * @memberof protocol.OnlineCountPayload
         * @instance
         */
        OnlineCountPayload.prototype.count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified OnlineCountPayload message. Does not implicitly {@link protocol.OnlineCountPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.OnlineCountPayload
         * @static
         * @param {protocol.OnlineCountPayload.$Properties} message OnlineCountPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OnlineCountPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.count != null && $Object.hasOwnProperty.call(message, "count") && (typeof message.count === "object" ? message.count.low || message.count.high : message.count !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.count);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes an OnlineCountPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.OnlineCountPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.OnlineCountPayload & protocol.OnlineCountPayload.$Shape} OnlineCountPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OnlineCountPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.OnlineCountPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies an OnlineCountPayload message.
         * @function verify
         * @memberof protocol.OnlineCountPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OnlineCountPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                if (!$util.isInteger(message.count) && !(message.count && $util.isInteger(message.count.low) && $util.isInteger(message.count.high)))
                    return "count: integer|Long expected";
            return null;
        };

        return OnlineCountPayload;
    })();

    protocol.MaintenanceStatusPayload = (function() {

        /**
         * Properties of a MaintenanceStatusPayload.
         * @typedef {Object} protocol.MaintenanceStatusPayload.$Properties
         * @property {boolean|null} [maintenance] MaintenanceStatusPayload maintenance
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a MaintenanceStatusPayload.
         * @memberof protocol
         * @interface IMaintenanceStatusPayload
         * @augments protocol.MaintenanceStatusPayload.$Properties
         * @deprecated Use protocol.MaintenanceStatusPayload.$Properties instead.
         */

        /**
         * Shape of a MaintenanceStatusPayload.
         * @typedef {protocol.MaintenanceStatusPayload.$Properties} protocol.MaintenanceStatusPayload.$Shape
         */

        /**
         * Constructs a new MaintenanceStatusPayload.
         * @memberof protocol
         * @classdesc Represents a MaintenanceStatusPayload.
         * @constructor
         * @param {protocol.MaintenanceStatusPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const MaintenanceStatusPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MaintenanceStatusPayload maintenance.
         * @member {boolean} maintenance
         * @memberof protocol.MaintenanceStatusPayload
         * @instance
         */
        MaintenanceStatusPayload.prototype.maintenance = false;

        /**
         * Encodes the specified MaintenanceStatusPayload message. Does not implicitly {@link protocol.MaintenanceStatusPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.MaintenanceStatusPayload
         * @static
         * @param {protocol.MaintenanceStatusPayload.$Properties} message MaintenanceStatusPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MaintenanceStatusPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.maintenance != null && $Object.hasOwnProperty.call(message, "maintenance") && message.maintenance !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.maintenance);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a MaintenanceStatusPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.MaintenanceStatusPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.MaintenanceStatusPayload & protocol.MaintenanceStatusPayload.$Shape} MaintenanceStatusPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MaintenanceStatusPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.MaintenanceStatusPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.maintenance = value;
                        else
                            delete message.maintenance;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a MaintenanceStatusPayload message.
         * @function verify
         * @memberof protocol.MaintenanceStatusPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MaintenanceStatusPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.maintenance != null && $Object.hasOwnProperty.call(message, "maintenance"))
                if (typeof message.maintenance !== "boolean")
                    return "maintenance: boolean expected";
            return null;
        };

        return MaintenanceStatusPayload;
    })();

    protocol.MaintenancePayload = (function() {

        /**
         * Properties of a MaintenancePayload.
         * @typedef {Object} protocol.MaintenancePayload.$Properties
         * @property {boolean|null} [maintenance] MaintenancePayload maintenance
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a MaintenancePayload.
         * @memberof protocol
         * @interface IMaintenancePayload
         * @augments protocol.MaintenancePayload.$Properties
         * @deprecated Use protocol.MaintenancePayload.$Properties instead.
         */

        /**
         * Shape of a MaintenancePayload.
         * @typedef {protocol.MaintenancePayload.$Properties} protocol.MaintenancePayload.$Shape
         */

        /**
         * Constructs a new MaintenancePayload.
         * @memberof protocol
         * @classdesc Represents a MaintenancePayload.
         * @constructor
         * @param {protocol.MaintenancePayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const MaintenancePayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MaintenancePayload maintenance.
         * @member {boolean} maintenance
         * @memberof protocol.MaintenancePayload
         * @instance
         */
        MaintenancePayload.prototype.maintenance = false;

        /**
         * Encodes the specified MaintenancePayload message. Does not implicitly {@link protocol.MaintenancePayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.MaintenancePayload
         * @static
         * @param {protocol.MaintenancePayload.$Properties} message MaintenancePayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MaintenancePayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.maintenance != null && $Object.hasOwnProperty.call(message, "maintenance") && message.maintenance !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.maintenance);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a MaintenancePayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.MaintenancePayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.MaintenancePayload & protocol.MaintenancePayload.$Shape} MaintenancePayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MaintenancePayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.MaintenancePayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.maintenance = value;
                        else
                            delete message.maintenance;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a MaintenancePayload message.
         * @function verify
         * @memberof protocol.MaintenancePayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MaintenancePayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.maintenance != null && $Object.hasOwnProperty.call(message, "maintenance"))
                if (typeof message.maintenance !== "boolean")
                    return "maintenance: boolean expected";
            return null;
        };

        return MaintenancePayload;
    })();

    protocol.ErrorPayload = (function() {

        /**
         * Properties of an ErrorPayload.
         * @typedef {Object} protocol.ErrorPayload.$Properties
         * @property {number|null} [code] ErrorPayload code
         * @property {string|null} [message] ErrorPayload message
         * @property {protocol.MessageType|null} [command_type] ErrorPayload command_type
         * @property {string|null} [request_id] ErrorPayload request_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an ErrorPayload.
         * @memberof protocol
         * @interface IErrorPayload
         * @augments protocol.ErrorPayload.$Properties
         * @deprecated Use protocol.ErrorPayload.$Properties instead.
         */

        /**
         * Shape of an ErrorPayload.
         * @typedef {protocol.ErrorPayload.$Properties} protocol.ErrorPayload.$Shape
         */

        /**
         * Constructs a new ErrorPayload.
         * @memberof protocol
         * @classdesc Represents an ErrorPayload.
         * @constructor
         * @param {protocol.ErrorPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ErrorPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ErrorPayload code.
         * @member {number} code
         * @memberof protocol.ErrorPayload
         * @instance
         */
        ErrorPayload.prototype.code = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ErrorPayload message.
         * @member {string} message
         * @memberof protocol.ErrorPayload
         * @instance
         */
        ErrorPayload.prototype.message = "";

        /**
         * ErrorPayload command_type.
         * @member {protocol.MessageType} command_type
         * @memberof protocol.ErrorPayload
         * @instance
         */
        ErrorPayload.prototype.command_type = 0;

        /**
         * ErrorPayload request_id.
         * @member {string} request_id
         * @memberof protocol.ErrorPayload
         * @instance
         */
        ErrorPayload.prototype.request_id = "";

        /**
         * Encodes the specified ErrorPayload message. Does not implicitly {@link protocol.ErrorPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.ErrorPayload
         * @static
         * @param {protocol.ErrorPayload.$Properties} message ErrorPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ErrorPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.code != null && $Object.hasOwnProperty.call(message, "code") && (typeof message.code === "object" ? message.code.low || message.code.high : message.code !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.code);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            if (message.command_type != null && $Object.hasOwnProperty.call(message, "command_type") && message.command_type !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.command_type);
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id") && message.request_id !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.request_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes an ErrorPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.ErrorPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.ErrorPayload & protocol.ErrorPayload.$Shape} ErrorPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ErrorPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.ErrorPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.command_type = value;
                        else
                            delete message.command_type;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.request_id = value;
                        else
                            delete message.request_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies an ErrorPayload message.
         * @function verify
         * @memberof protocol.ErrorPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ErrorPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                if (!$util.isInteger(message.code) && !(message.code && $util.isInteger(message.code.low) && $util.isInteger(message.code.high)))
                    return "code: integer|Long expected";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            if (message.command_type != null && $Object.hasOwnProperty.call(message, "command_type"))
                if (typeof message.command_type !== "number" || (message.command_type | 0) !== message.command_type)
                    return "command_type: enum value expected";
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                if (!$util.isString(message.request_id))
                    return "request_id: string expected";
            return null;
        };

        return ErrorPayload;
    })();

    protocol.MatchQueuedPayload = (function() {

        /**
         * Properties of a MatchQueuedPayload.
         * @typedef {Object} protocol.MatchQueuedPayload.$Properties
         * @property {number|null} [deadline_ms] MatchQueuedPayload deadline_ms
         * @property {boolean|null} [practice] MatchQueuedPayload practice
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a MatchQueuedPayload.
         * @memberof protocol
         * @interface IMatchQueuedPayload
         * @augments protocol.MatchQueuedPayload.$Properties
         * @deprecated Use protocol.MatchQueuedPayload.$Properties instead.
         */

        /**
         * Shape of a MatchQueuedPayload.
         * @typedef {protocol.MatchQueuedPayload.$Properties} protocol.MatchQueuedPayload.$Shape
         */

        /**
         * Constructs a new MatchQueuedPayload.
         * @memberof protocol
         * @classdesc Represents a MatchQueuedPayload.
         * @constructor
         * @param {protocol.MatchQueuedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const MatchQueuedPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MatchQueuedPayload deadline_ms.
         * @member {number} deadline_ms
         * @memberof protocol.MatchQueuedPayload
         * @instance
         */
        MatchQueuedPayload.prototype.deadline_ms = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MatchQueuedPayload practice.
         * @member {boolean} practice
         * @memberof protocol.MatchQueuedPayload
         * @instance
         */
        MatchQueuedPayload.prototype.practice = false;

        /**
         * Encodes the specified MatchQueuedPayload message. Does not implicitly {@link protocol.MatchQueuedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.MatchQueuedPayload
         * @static
         * @param {protocol.MatchQueuedPayload.$Properties} message MatchQueuedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MatchQueuedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.deadline_ms != null && $Object.hasOwnProperty.call(message, "deadline_ms") && (typeof message.deadline_ms === "object" ? message.deadline_ms.low || message.deadline_ms.high : message.deadline_ms !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.deadline_ms);
            if (message.practice != null && $Object.hasOwnProperty.call(message, "practice") && message.practice !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.practice);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a MatchQueuedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.MatchQueuedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.MatchQueuedPayload & protocol.MatchQueuedPayload.$Shape} MatchQueuedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MatchQueuedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.MatchQueuedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.deadline_ms = value;
                        else
                            delete message.deadline_ms;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.practice = value;
                        else
                            delete message.practice;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a MatchQueuedPayload message.
         * @function verify
         * @memberof protocol.MatchQueuedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MatchQueuedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.deadline_ms != null && $Object.hasOwnProperty.call(message, "deadline_ms"))
                if (!$util.isInteger(message.deadline_ms) && !(message.deadline_ms && $util.isInteger(message.deadline_ms.low) && $util.isInteger(message.deadline_ms.high)))
                    return "deadline_ms: integer|Long expected";
            if (message.practice != null && $Object.hasOwnProperty.call(message, "practice"))
                if (typeof message.practice !== "boolean")
                    return "practice: boolean expected";
            return null;
        };

        return MatchQueuedPayload;
    })();

    protocol.MatchCancelledPayload = (function() {

        /**
         * Properties of a MatchCancelledPayload.
         * @typedef {Object} protocol.MatchCancelledPayload.$Properties
         * @property {string|null} [reason] MatchCancelledPayload reason
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a MatchCancelledPayload.
         * @memberof protocol
         * @interface IMatchCancelledPayload
         * @augments protocol.MatchCancelledPayload.$Properties
         * @deprecated Use protocol.MatchCancelledPayload.$Properties instead.
         */

        /**
         * Shape of a MatchCancelledPayload.
         * @typedef {protocol.MatchCancelledPayload.$Properties} protocol.MatchCancelledPayload.$Shape
         */

        /**
         * Constructs a new MatchCancelledPayload.
         * @memberof protocol
         * @classdesc Represents a MatchCancelledPayload.
         * @constructor
         * @param {protocol.MatchCancelledPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const MatchCancelledPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MatchCancelledPayload reason.
         * @member {string} reason
         * @memberof protocol.MatchCancelledPayload
         * @instance
         */
        MatchCancelledPayload.prototype.reason = "";

        /**
         * Encodes the specified MatchCancelledPayload message. Does not implicitly {@link protocol.MatchCancelledPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.MatchCancelledPayload
         * @static
         * @param {protocol.MatchCancelledPayload.$Properties} message MatchCancelledPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MatchCancelledPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.reason != null && $Object.hasOwnProperty.call(message, "reason") && message.reason !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.reason);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a MatchCancelledPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.MatchCancelledPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.MatchCancelledPayload & protocol.MatchCancelledPayload.$Shape} MatchCancelledPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MatchCancelledPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.MatchCancelledPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.reason = value;
                        else
                            delete message.reason;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a MatchCancelledPayload message.
         * @function verify
         * @memberof protocol.MatchCancelledPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MatchCancelledPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.reason != null && $Object.hasOwnProperty.call(message, "reason"))
                if (!$util.isString(message.reason))
                    return "reason: string expected";
            return null;
        };

        return MatchCancelledPayload;
    })();

    protocol.RoomLeftPayload = (function() {

        /**
         * Properties of a RoomLeftPayload.
         * @typedef {Object} protocol.RoomLeftPayload.$Properties
         * @property {string|null} [room_code] RoomLeftPayload room_code
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomLeftPayload.
         * @memberof protocol
         * @interface IRoomLeftPayload
         * @augments protocol.RoomLeftPayload.$Properties
         * @deprecated Use protocol.RoomLeftPayload.$Properties instead.
         */

        /**
         * Shape of a RoomLeftPayload.
         * @typedef {protocol.RoomLeftPayload.$Properties} protocol.RoomLeftPayload.$Shape
         */

        /**
         * Constructs a new RoomLeftPayload.
         * @memberof protocol
         * @classdesc Represents a RoomLeftPayload.
         * @constructor
         * @param {protocol.RoomLeftPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomLeftPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomLeftPayload room_code.
         * @member {string} room_code
         * @memberof protocol.RoomLeftPayload
         * @instance
         */
        RoomLeftPayload.prototype.room_code = "";

        /**
         * Encodes the specified RoomLeftPayload message. Does not implicitly {@link protocol.RoomLeftPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.RoomLeftPayload
         * @static
         * @param {protocol.RoomLeftPayload.$Properties} message RoomLeftPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomLeftPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code") && message.room_code !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.room_code);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a RoomLeftPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.RoomLeftPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.RoomLeftPayload & protocol.RoomLeftPayload.$Shape} RoomLeftPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomLeftPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.RoomLeftPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.room_code = value;
                        else
                            delete message.room_code;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a RoomLeftPayload message.
         * @function verify
         * @memberof protocol.RoomLeftPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomLeftPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code"))
                if (!$util.isString(message.room_code))
                    return "room_code: string expected";
            return null;
        };

        return RoomLeftPayload;
    })();

    protocol.StatsResultPayload = (function() {

        /**
         * Properties of a StatsResultPayload.
         * @typedef {Object} protocol.StatsResultPayload.$Properties
         * @property {string|null} [player_id] StatsResultPayload player_id
         * @property {string|null} [player_name] StatsResultPayload player_name
         * @property {number|null} [total_games] StatsResultPayload total_games
         * @property {number|null} [wins] StatsResultPayload wins
         * @property {number|null} [losses] StatsResultPayload losses
         * @property {number|null} [win_rate] StatsResultPayload win_rate
         * @property {number|null} [landlord_games] StatsResultPayload landlord_games
         * @property {number|null} [landlord_wins] StatsResultPayload landlord_wins
         * @property {number|null} [farmer_games] StatsResultPayload farmer_games
         * @property {number|null} [farmer_wins] StatsResultPayload farmer_wins
         * @property {number|null} [score] StatsResultPayload score
         * @property {number|null} [rank] StatsResultPayload rank
         * @property {number|null} [current_streak] StatsResultPayload current_streak
         * @property {number|null} [max_win_streak] StatsResultPayload max_win_streak
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a StatsResultPayload.
         * @memberof protocol
         * @interface IStatsResultPayload
         * @augments protocol.StatsResultPayload.$Properties
         * @deprecated Use protocol.StatsResultPayload.$Properties instead.
         */

        /**
         * Shape of a StatsResultPayload.
         * @typedef {protocol.StatsResultPayload.$Properties} protocol.StatsResultPayload.$Shape
         */

        /**
         * Constructs a new StatsResultPayload.
         * @memberof protocol
         * @classdesc Represents a StatsResultPayload.
         * @constructor
         * @param {protocol.StatsResultPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const StatsResultPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StatsResultPayload player_id.
         * @member {string} player_id
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.player_id = "";

        /**
         * StatsResultPayload player_name.
         * @member {string} player_name
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.player_name = "";

        /**
         * StatsResultPayload total_games.
         * @member {number} total_games
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.total_games = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload wins.
         * @member {number} wins
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.wins = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload losses.
         * @member {number} losses
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.losses = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload win_rate.
         * @member {number} win_rate
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.win_rate = 0;

        /**
         * StatsResultPayload landlord_games.
         * @member {number} landlord_games
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.landlord_games = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload landlord_wins.
         * @member {number} landlord_wins
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.landlord_wins = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload farmer_games.
         * @member {number} farmer_games
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.farmer_games = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload farmer_wins.
         * @member {number} farmer_wins
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.farmer_wins = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload score.
         * @member {number} score
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.score = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload rank.
         * @member {number} rank
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.rank = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload current_streak.
         * @member {number} current_streak
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.current_streak = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * StatsResultPayload max_win_streak.
         * @member {number} max_win_streak
         * @memberof protocol.StatsResultPayload
         * @instance
         */
        StatsResultPayload.prototype.max_win_streak = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified StatsResultPayload message. Does not implicitly {@link protocol.StatsResultPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.StatsResultPayload
         * @static
         * @param {protocol.StatsResultPayload.$Properties} message StatsResultPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatsResultPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.total_games != null && $Object.hasOwnProperty.call(message, "total_games") && (typeof message.total_games === "object" ? message.total_games.low || message.total_games.high : message.total_games !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.total_games);
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins") && (typeof message.wins === "object" ? message.wins.low || message.wins.high : message.wins !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.wins);
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses") && (typeof message.losses === "object" ? message.losses.low || message.losses.high : message.losses !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.losses);
            if (message.win_rate != null && $Object.hasOwnProperty.call(message, "win_rate") && !$Object.is(message.win_rate, 0))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.win_rate);
            if (message.landlord_games != null && $Object.hasOwnProperty.call(message, "landlord_games") && (typeof message.landlord_games === "object" ? message.landlord_games.low || message.landlord_games.high : message.landlord_games !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.landlord_games);
            if (message.landlord_wins != null && $Object.hasOwnProperty.call(message, "landlord_wins") && (typeof message.landlord_wins === "object" ? message.landlord_wins.low || message.landlord_wins.high : message.landlord_wins !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.landlord_wins);
            if (message.farmer_games != null && $Object.hasOwnProperty.call(message, "farmer_games") && (typeof message.farmer_games === "object" ? message.farmer_games.low || message.farmer_games.high : message.farmer_games !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.farmer_games);
            if (message.farmer_wins != null && $Object.hasOwnProperty.call(message, "farmer_wins") && (typeof message.farmer_wins === "object" ? message.farmer_wins.low || message.farmer_wins.high : message.farmer_wins !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.farmer_wins);
            if (message.score != null && $Object.hasOwnProperty.call(message, "score") && (typeof message.score === "object" ? message.score.low || message.score.high : message.score !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.score);
            if (message.rank != null && $Object.hasOwnProperty.call(message, "rank") && (typeof message.rank === "object" ? message.rank.low || message.rank.high : message.rank !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.rank);
            if (message.current_streak != null && $Object.hasOwnProperty.call(message, "current_streak") && (typeof message.current_streak === "object" ? message.current_streak.low || message.current_streak.high : message.current_streak !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.current_streak);
            if (message.max_win_streak != null && $Object.hasOwnProperty.call(message, "max_win_streak") && (typeof message.max_win_streak === "object" ? message.max_win_streak.low || message.max_win_streak.high : message.max_win_streak !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.max_win_streak);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a StatsResultPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.StatsResultPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.StatsResultPayload & protocol.StatsResultPayload.$Shape} StatsResultPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatsResultPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.StatsResultPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.total_games = value;
                        else
                            delete message.total_games;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.wins = value;
                        else
                            delete message.wins;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.losses = value;
                        else
                            delete message.losses;
                        continue;
                    }
                case 6: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.win_rate = value;
                        else
                            delete message.win_rate;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.landlord_games = value;
                        else
                            delete message.landlord_games;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.landlord_wins = value;
                        else
                            delete message.landlord_wins;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.farmer_games = value;
                        else
                            delete message.farmer_games;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.farmer_wins = value;
                        else
                            delete message.farmer_wins;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.rank = value;
                        else
                            delete message.rank;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.current_streak = value;
                        else
                            delete message.current_streak;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.max_win_streak = value;
                        else
                            delete message.max_win_streak;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a StatsResultPayload message.
         * @function verify
         * @memberof protocol.StatsResultPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatsResultPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.total_games != null && $Object.hasOwnProperty.call(message, "total_games"))
                if (!$util.isInteger(message.total_games) && !(message.total_games && $util.isInteger(message.total_games.low) && $util.isInteger(message.total_games.high)))
                    return "total_games: integer|Long expected";
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                if (!$util.isInteger(message.wins) && !(message.wins && $util.isInteger(message.wins.low) && $util.isInteger(message.wins.high)))
                    return "wins: integer|Long expected";
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                if (!$util.isInteger(message.losses) && !(message.losses && $util.isInteger(message.losses.low) && $util.isInteger(message.losses.high)))
                    return "losses: integer|Long expected";
            if (message.win_rate != null && $Object.hasOwnProperty.call(message, "win_rate"))
                if (typeof message.win_rate !== "number")
                    return "win_rate: number expected";
            if (message.landlord_games != null && $Object.hasOwnProperty.call(message, "landlord_games"))
                if (!$util.isInteger(message.landlord_games) && !(message.landlord_games && $util.isInteger(message.landlord_games.low) && $util.isInteger(message.landlord_games.high)))
                    return "landlord_games: integer|Long expected";
            if (message.landlord_wins != null && $Object.hasOwnProperty.call(message, "landlord_wins"))
                if (!$util.isInteger(message.landlord_wins) && !(message.landlord_wins && $util.isInteger(message.landlord_wins.low) && $util.isInteger(message.landlord_wins.high)))
                    return "landlord_wins: integer|Long expected";
            if (message.farmer_games != null && $Object.hasOwnProperty.call(message, "farmer_games"))
                if (!$util.isInteger(message.farmer_games) && !(message.farmer_games && $util.isInteger(message.farmer_games.low) && $util.isInteger(message.farmer_games.high)))
                    return "farmer_games: integer|Long expected";
            if (message.farmer_wins != null && $Object.hasOwnProperty.call(message, "farmer_wins"))
                if (!$util.isInteger(message.farmer_wins) && !(message.farmer_wins && $util.isInteger(message.farmer_wins.low) && $util.isInteger(message.farmer_wins.high)))
                    return "farmer_wins: integer|Long expected";
            if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
                if (!$util.isInteger(message.score) && !(message.score && $util.isInteger(message.score.low) && $util.isInteger(message.score.high)))
                    return "score: integer|Long expected";
            if (message.rank != null && $Object.hasOwnProperty.call(message, "rank"))
                if (!$util.isInteger(message.rank) && !(message.rank && $util.isInteger(message.rank.low) && $util.isInteger(message.rank.high)))
                    return "rank: integer|Long expected";
            if (message.current_streak != null && $Object.hasOwnProperty.call(message, "current_streak"))
                if (!$util.isInteger(message.current_streak) && !(message.current_streak && $util.isInteger(message.current_streak.low) && $util.isInteger(message.current_streak.high)))
                    return "current_streak: integer|Long expected";
            if (message.max_win_streak != null && $Object.hasOwnProperty.call(message, "max_win_streak"))
                if (!$util.isInteger(message.max_win_streak) && !(message.max_win_streak && $util.isInteger(message.max_win_streak.low) && $util.isInteger(message.max_win_streak.high)))
                    return "max_win_streak: integer|Long expected";
            return null;
        };

        return StatsResultPayload;
    })();

    protocol.LeaderboardResultPayload = (function() {

        /**
         * Properties of a LeaderboardResultPayload.
         * @typedef {Object} protocol.LeaderboardResultPayload.$Properties
         * @property {string|null} [type] LeaderboardResultPayload type
         * @property {Array.<protocol.LeaderboardEntry.$Properties>|null} [entries] LeaderboardResultPayload entries
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LeaderboardResultPayload.
         * @memberof protocol
         * @interface ILeaderboardResultPayload
         * @augments protocol.LeaderboardResultPayload.$Properties
         * @deprecated Use protocol.LeaderboardResultPayload.$Properties instead.
         */

        /**
         * Shape of a LeaderboardResultPayload.
         * @typedef {protocol.LeaderboardResultPayload.$Properties} protocol.LeaderboardResultPayload.$Shape
         */

        /**
         * Constructs a new LeaderboardResultPayload.
         * @memberof protocol
         * @classdesc Represents a LeaderboardResultPayload.
         * @constructor
         * @param {protocol.LeaderboardResultPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LeaderboardResultPayload = function (properties) {
            this.entries = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LeaderboardResultPayload type.
         * @member {string} type
         * @memberof protocol.LeaderboardResultPayload
         * @instance
         */
        LeaderboardResultPayload.prototype.type = "";

        /**
         * LeaderboardResultPayload entries.
         * @member {Array.<protocol.LeaderboardEntry.$Properties>} entries
         * @memberof protocol.LeaderboardResultPayload
         * @instance
         */
        LeaderboardResultPayload.prototype.entries = $util.emptyArray;

        /**
         * Encodes the specified LeaderboardResultPayload message. Does not implicitly {@link protocol.LeaderboardResultPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.LeaderboardResultPayload
         * @static
         * @param {protocol.LeaderboardResultPayload.$Properties} message LeaderboardResultPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LeaderboardResultPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.type);
            if (message.entries != null && message.entries.length)
                for (let i = 0; i < message.entries.length; ++i)
                    $root.protocol.LeaderboardEntry.encode(message.entries[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a LeaderboardResultPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.LeaderboardResultPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.LeaderboardResultPayload & protocol.LeaderboardResultPayload.$Shape} LeaderboardResultPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LeaderboardResultPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.LeaderboardResultPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.entries && message.entries.length))
                            message.entries = [];
                        message.entries.push($root.protocol.LeaderboardEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a LeaderboardResultPayload message.
         * @function verify
         * @memberof protocol.LeaderboardResultPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LeaderboardResultPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.entries != null && $Object.hasOwnProperty.call(message, "entries")) {
                if (!$Array.isArray(message.entries))
                    return "entries: array expected";
                for (let i = 0; i < message.entries.length; ++i) {
                    let error = $root.protocol.LeaderboardEntry.verify(message.entries[i], _depth + 1);
                    if (error)
                        return "entries." + error;
                }
            }
            return null;
        };

        return LeaderboardResultPayload;
    })();

    protocol.RoomListResultPayload = (function() {

        /**
         * Properties of a RoomListResultPayload.
         * @typedef {Object} protocol.RoomListResultPayload.$Properties
         * @property {Array.<protocol.RoomListItem.$Properties>|null} [rooms] RoomListResultPayload rooms
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomListResultPayload.
         * @memberof protocol
         * @interface IRoomListResultPayload
         * @augments protocol.RoomListResultPayload.$Properties
         * @deprecated Use protocol.RoomListResultPayload.$Properties instead.
         */

        /**
         * Shape of a RoomListResultPayload.
         * @typedef {protocol.RoomListResultPayload.$Properties} protocol.RoomListResultPayload.$Shape
         */

        /**
         * Constructs a new RoomListResultPayload.
         * @memberof protocol
         * @classdesc Represents a RoomListResultPayload.
         * @constructor
         * @param {protocol.RoomListResultPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomListResultPayload = function (properties) {
            this.rooms = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomListResultPayload rooms.
         * @member {Array.<protocol.RoomListItem.$Properties>} rooms
         * @memberof protocol.RoomListResultPayload
         * @instance
         */
        RoomListResultPayload.prototype.rooms = $util.emptyArray;

        /**
         * Encodes the specified RoomListResultPayload message. Does not implicitly {@link protocol.RoomListResultPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.RoomListResultPayload
         * @static
         * @param {protocol.RoomListResultPayload.$Properties} message RoomListResultPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomListResultPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.rooms != null && message.rooms.length)
                for (let i = 0; i < message.rooms.length; ++i)
                    $root.protocol.RoomListItem.encode(message.rooms[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a RoomListResultPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.RoomListResultPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.RoomListResultPayload & protocol.RoomListResultPayload.$Shape} RoomListResultPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomListResultPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.RoomListResultPayload();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rooms && message.rooms.length))
                            message.rooms = [];
                        message.rooms.push($root.protocol.RoomListItem.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a RoomListResultPayload message.
         * @function verify
         * @memberof protocol.RoomListResultPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomListResultPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.rooms != null && $Object.hasOwnProperty.call(message, "rooms")) {
                if (!$Array.isArray(message.rooms))
                    return "rooms: array expected";
                for (let i = 0; i < message.rooms.length; ++i) {
                    let error = $root.protocol.RoomListItem.verify(message.rooms[i], _depth + 1);
                    if (error)
                        return "rooms." + error;
                }
            }
            return null;
        };

        return RoomListResultPayload;
    })();

    /**
     * MessageType enum.
     * @name protocol.MessageType
     * @enum {number}
     * @property {number} MSG_UNKNOWN=0 MSG_UNKNOWN value
     * @property {number} MSG_RECONNECT=1 MSG_RECONNECT value
     * @property {number} MSG_PING=2 MSG_PING value
     * @property {number} MSG_CREATE_ROOM=3 MSG_CREATE_ROOM value
     * @property {number} MSG_JOIN_ROOM=4 MSG_JOIN_ROOM value
     * @property {number} MSG_LEAVE_ROOM=5 MSG_LEAVE_ROOM value
     * @property {number} MSG_QUICK_MATCH=6 MSG_QUICK_MATCH value
     * @property {number} MSG_READY=7 MSG_READY value
     * @property {number} MSG_CANCEL_READY=8 MSG_CANCEL_READY value
     * @property {number} MSG_BID=9 MSG_BID value
     * @property {number} MSG_PLAY_CARDS=10 MSG_PLAY_CARDS value
     * @property {number} MSG_PASS=11 MSG_PASS value
     * @property {number} MSG_GET_STATS=12 MSG_GET_STATS value
     * @property {number} MSG_GET_LEADERBOARD=13 MSG_GET_LEADERBOARD value
     * @property {number} MSG_GET_ROOM_LIST=14 MSG_GET_ROOM_LIST value
     * @property {number} MSG_GET_ONLINE_COUNT=15 MSG_GET_ONLINE_COUNT value
     * @property {number} MSG_CHAT=16 MSG_CHAT value
     * @property {number} MSG_GET_MAINTENANCE_STATUS=17 MSG_GET_MAINTENANCE_STATUS value
     * @property {number} MSG_CANCEL_MATCH=18 MSG_CANCEL_MATCH value
     * @property {number} MSG_HELLO=19 MSG_HELLO value
     * @property {number} MSG_CONNECTED=100 MSG_CONNECTED value
     * @property {number} MSG_RECONNECTED=101 MSG_RECONNECTED value
     * @property {number} MSG_PONG=102 MSG_PONG value
     * @property {number} MSG_PLAYER_OFFLINE=103 MSG_PLAYER_OFFLINE value
     * @property {number} MSG_PLAYER_ONLINE=104 MSG_PLAYER_ONLINE value
     * @property {number} MSG_ONLINE_COUNT=105 MSG_ONLINE_COUNT value
     * @property {number} MSG_ROOM_CREATED=106 MSG_ROOM_CREATED value
     * @property {number} MSG_ROOM_JOINED=107 MSG_ROOM_JOINED value
     * @property {number} MSG_PLAYER_JOINED=108 MSG_PLAYER_JOINED value
     * @property {number} MSG_PLAYER_LEFT=109 MSG_PLAYER_LEFT value
     * @property {number} MSG_PLAYER_READY=110 MSG_PLAYER_READY value
     * @property {number} MSG_MATCH_FOUND=111 MSG_MATCH_FOUND value
     * @property {number} MSG_GAME_START=112 MSG_GAME_START value
     * @property {number} MSG_DEAL_CARDS=113 MSG_DEAL_CARDS value
     * @property {number} MSG_BID_TURN=114 MSG_BID_TURN value
     * @property {number} MSG_BID_RESULT=115 MSG_BID_RESULT value
     * @property {number} MSG_LANDLORD=116 MSG_LANDLORD value
     * @property {number} MSG_PLAY_TURN=117 MSG_PLAY_TURN value
     * @property {number} MSG_CARD_PLAYED=118 MSG_CARD_PLAYED value
     * @property {number} MSG_PLAYER_PASS=119 MSG_PLAYER_PASS value
     * @property {number} MSG_GAME_OVER=120 MSG_GAME_OVER value
     * @property {number} MSG_ROUND_RESULT=121 MSG_ROUND_RESULT value
     * @property {number} MSG_STATS_RESULT=122 MSG_STATS_RESULT value
     * @property {number} MSG_LEADERBOARD_RESULT=123 MSG_LEADERBOARD_RESULT value
     * @property {number} MSG_ROOM_LIST_RESULT=124 MSG_ROOM_LIST_RESULT value
     * @property {number} MSG_MAINTENANCE_STATUS=125 MSG_MAINTENANCE_STATUS value
     * @property {number} MSG_MAINTENANCE=126 MSG_MAINTENANCE value
     * @property {number} MSG_MATCH_QUEUED=127 MSG_MATCH_QUEUED value
     * @property {number} MSG_MATCH_CANCELLED=128 MSG_MATCH_CANCELLED value
     * @property {number} MSG_ROOM_LEFT=129 MSG_ROOM_LEFT value
     * @property {number} MSG_NEGOTIATED=130 MSG_NEGOTIATED value
     * @property {number} MSG_PROTOCOL_REJECTED=131 MSG_PROTOCOL_REJECTED value
     * @property {number} MSG_COMMAND_ACK=132 MSG_COMMAND_ACK value
     * @property {number} MSG_WARNING=133 MSG_WARNING value
     * @property {number} MSG_ERROR=200 MSG_ERROR value
     * @property {number} MSG_PRACTICE_MATCH=201 MSG_PRACTICE_MATCH value
     */
    protocol.MessageType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "MSG_UNKNOWN"] = 0;
        values[valuesById[1] = "MSG_RECONNECT"] = 1;
        values[valuesById[2] = "MSG_PING"] = 2;
        values[valuesById[3] = "MSG_CREATE_ROOM"] = 3;
        values[valuesById[4] = "MSG_JOIN_ROOM"] = 4;
        values[valuesById[5] = "MSG_LEAVE_ROOM"] = 5;
        values[valuesById[6] = "MSG_QUICK_MATCH"] = 6;
        values[valuesById[7] = "MSG_READY"] = 7;
        values[valuesById[8] = "MSG_CANCEL_READY"] = 8;
        values[valuesById[9] = "MSG_BID"] = 9;
        values[valuesById[10] = "MSG_PLAY_CARDS"] = 10;
        values[valuesById[11] = "MSG_PASS"] = 11;
        values[valuesById[12] = "MSG_GET_STATS"] = 12;
        values[valuesById[13] = "MSG_GET_LEADERBOARD"] = 13;
        values[valuesById[14] = "MSG_GET_ROOM_LIST"] = 14;
        values[valuesById[15] = "MSG_GET_ONLINE_COUNT"] = 15;
        values[valuesById[16] = "MSG_CHAT"] = 16;
        values[valuesById[17] = "MSG_GET_MAINTENANCE_STATUS"] = 17;
        values[valuesById[18] = "MSG_CANCEL_MATCH"] = 18;
        values[valuesById[19] = "MSG_HELLO"] = 19;
        values[valuesById[100] = "MSG_CONNECTED"] = 100;
        values[valuesById[101] = "MSG_RECONNECTED"] = 101;
        values[valuesById[102] = "MSG_PONG"] = 102;
        values[valuesById[103] = "MSG_PLAYER_OFFLINE"] = 103;
        values[valuesById[104] = "MSG_PLAYER_ONLINE"] = 104;
        values[valuesById[105] = "MSG_ONLINE_COUNT"] = 105;
        values[valuesById[106] = "MSG_ROOM_CREATED"] = 106;
        values[valuesById[107] = "MSG_ROOM_JOINED"] = 107;
        values[valuesById[108] = "MSG_PLAYER_JOINED"] = 108;
        values[valuesById[109] = "MSG_PLAYER_LEFT"] = 109;
        values[valuesById[110] = "MSG_PLAYER_READY"] = 110;
        values[valuesById[111] = "MSG_MATCH_FOUND"] = 111;
        values[valuesById[112] = "MSG_GAME_START"] = 112;
        values[valuesById[113] = "MSG_DEAL_CARDS"] = 113;
        values[valuesById[114] = "MSG_BID_TURN"] = 114;
        values[valuesById[115] = "MSG_BID_RESULT"] = 115;
        values[valuesById[116] = "MSG_LANDLORD"] = 116;
        values[valuesById[117] = "MSG_PLAY_TURN"] = 117;
        values[valuesById[118] = "MSG_CARD_PLAYED"] = 118;
        values[valuesById[119] = "MSG_PLAYER_PASS"] = 119;
        values[valuesById[120] = "MSG_GAME_OVER"] = 120;
        values[valuesById[121] = "MSG_ROUND_RESULT"] = 121;
        values[valuesById[122] = "MSG_STATS_RESULT"] = 122;
        values[valuesById[123] = "MSG_LEADERBOARD_RESULT"] = 123;
        values[valuesById[124] = "MSG_ROOM_LIST_RESULT"] = 124;
        values[valuesById[125] = "MSG_MAINTENANCE_STATUS"] = 125;
        values[valuesById[126] = "MSG_MAINTENANCE"] = 126;
        values[valuesById[127] = "MSG_MATCH_QUEUED"] = 127;
        values[valuesById[128] = "MSG_MATCH_CANCELLED"] = 128;
        values[valuesById[129] = "MSG_ROOM_LEFT"] = 129;
        values[valuesById[130] = "MSG_NEGOTIATED"] = 130;
        values[valuesById[131] = "MSG_PROTOCOL_REJECTED"] = 131;
        values[valuesById[132] = "MSG_COMMAND_ACK"] = 132;
        values[valuesById[133] = "MSG_WARNING"] = 133;
        values[valuesById[200] = "MSG_ERROR"] = 200;
        values[valuesById[201] = "MSG_PRACTICE_MATCH"] = 201;
        return values;
    })();

    protocol.EventMeta = (function() {

        /**
         * Properties of an EventMeta.
         * @typedef {Object} protocol.EventMeta.$Properties
         * @property {string|null} [stream_id] EventMeta stream_id
         * @property {number|null} [event_version] EventMeta event_version
         * @property {string|null} [game_id] EventMeta game_id
         * @property {number|null} [turn_id] EventMeta turn_id
         * @property {number|null} [server_time_ms] EventMeta server_time_ms
         * @property {number|null} [turn_deadline_ms] EventMeta turn_deadline_ms
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EventMeta.
         * @memberof protocol
         * @interface IEventMeta
         * @augments protocol.EventMeta.$Properties
         * @deprecated Use protocol.EventMeta.$Properties instead.
         */

        /**
         * Shape of an EventMeta.
         * @typedef {protocol.EventMeta.$Properties} protocol.EventMeta.$Shape
         */

        /**
         * Constructs a new EventMeta.
         * @memberof protocol
         * @classdesc Represents an EventMeta.
         * @constructor
         * @param {protocol.EventMeta.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EventMeta = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * EventMeta stream_id.
         * @member {string} stream_id
         * @memberof protocol.EventMeta
         * @instance
         */
        EventMeta.prototype.stream_id = "";

        /**
         * EventMeta event_version.
         * @member {number} event_version
         * @memberof protocol.EventMeta
         * @instance
         */
        EventMeta.prototype.event_version = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EventMeta game_id.
         * @member {string} game_id
         * @memberof protocol.EventMeta
         * @instance
         */
        EventMeta.prototype.game_id = "";

        /**
         * EventMeta turn_id.
         * @member {number} turn_id
         * @memberof protocol.EventMeta
         * @instance
         */
        EventMeta.prototype.turn_id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EventMeta server_time_ms.
         * @member {number} server_time_ms
         * @memberof protocol.EventMeta
         * @instance
         */
        EventMeta.prototype.server_time_ms = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EventMeta turn_deadline_ms.
         * @member {number} turn_deadline_ms
         * @memberof protocol.EventMeta
         * @instance
         */
        EventMeta.prototype.turn_deadline_ms = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified EventMeta message. Does not implicitly {@link protocol.EventMeta.verify|verify} messages.
         * @function encode
         * @memberof protocol.EventMeta
         * @static
         * @param {protocol.EventMeta.$Properties} message EventMeta message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventMeta.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id") && message.stream_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.stream_id);
            if (message.event_version != null && $Object.hasOwnProperty.call(message, "event_version") && (typeof message.event_version === "object" ? message.event_version.low || message.event_version.high : message.event_version !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.event_version);
            if (message.game_id != null && $Object.hasOwnProperty.call(message, "game_id") && message.game_id !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.game_id);
            if (message.turn_id != null && $Object.hasOwnProperty.call(message, "turn_id") && (typeof message.turn_id === "object" ? message.turn_id.low || message.turn_id.high : message.turn_id !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.turn_id);
            if (message.server_time_ms != null && $Object.hasOwnProperty.call(message, "server_time_ms") && (typeof message.server_time_ms === "object" ? message.server_time_ms.low || message.server_time_ms.high : message.server_time_ms !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.server_time_ms);
            if (message.turn_deadline_ms != null && $Object.hasOwnProperty.call(message, "turn_deadline_ms") && (typeof message.turn_deadline_ms === "object" ? message.turn_deadline_ms.low || message.turn_deadline_ms.high : message.turn_deadline_ms !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.turn_deadline_ms);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes an EventMeta message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.EventMeta
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.EventMeta & protocol.EventMeta.$Shape} EventMeta
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventMeta.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.EventMeta(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.stream_id = value;
                        else
                            delete message.stream_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.event_version = value;
                        else
                            delete message.event_version;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.game_id = value;
                        else
                            delete message.game_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.turn_id = value;
                        else
                            delete message.turn_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.server_time_ms = value;
                        else
                            delete message.server_time_ms;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.turn_deadline_ms = value;
                        else
                            delete message.turn_deadline_ms;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies an EventMeta message.
         * @function verify
         * @memberof protocol.EventMeta
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EventMeta.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id"))
                if (!$util.isString(message.stream_id))
                    return "stream_id: string expected";
            if (message.event_version != null && $Object.hasOwnProperty.call(message, "event_version"))
                if (!$util.isInteger(message.event_version) && !(message.event_version && $util.isInteger(message.event_version.low) && $util.isInteger(message.event_version.high)))
                    return "event_version: integer|Long expected";
            if (message.game_id != null && $Object.hasOwnProperty.call(message, "game_id"))
                if (!$util.isString(message.game_id))
                    return "game_id: string expected";
            if (message.turn_id != null && $Object.hasOwnProperty.call(message, "turn_id"))
                if (!$util.isInteger(message.turn_id) && !(message.turn_id && $util.isInteger(message.turn_id.low) && $util.isInteger(message.turn_id.high)))
                    return "turn_id: integer|Long expected";
            if (message.server_time_ms != null && $Object.hasOwnProperty.call(message, "server_time_ms"))
                if (!$util.isInteger(message.server_time_ms) && !(message.server_time_ms && $util.isInteger(message.server_time_ms.low) && $util.isInteger(message.server_time_ms.high)))
                    return "server_time_ms: integer|Long expected";
            if (message.turn_deadline_ms != null && $Object.hasOwnProperty.call(message, "turn_deadline_ms"))
                if (!$util.isInteger(message.turn_deadline_ms) && !(message.turn_deadline_ms && $util.isInteger(message.turn_deadline_ms.low) && $util.isInteger(message.turn_deadline_ms.high)))
                    return "turn_deadline_ms: integer|Long expected";
            return null;
        };

        return EventMeta;
    })();

    protocol.CommandMeta = (function() {

        /**
         * Properties of a CommandMeta.
         * @typedef {Object} protocol.CommandMeta.$Properties
         * @property {string|null} [request_id] CommandMeta request_id
         * @property {string|null} [expected_game_id] CommandMeta expected_game_id
         * @property {number|null} [expected_turn_id] CommandMeta expected_turn_id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CommandMeta.
         * @memberof protocol
         * @interface ICommandMeta
         * @augments protocol.CommandMeta.$Properties
         * @deprecated Use protocol.CommandMeta.$Properties instead.
         */

        /**
         * Shape of a CommandMeta.
         * @typedef {protocol.CommandMeta.$Properties} protocol.CommandMeta.$Shape
         */

        /**
         * Constructs a new CommandMeta.
         * @memberof protocol
         * @classdesc Represents a CommandMeta.
         * @constructor
         * @param {protocol.CommandMeta.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CommandMeta = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CommandMeta request_id.
         * @member {string} request_id
         * @memberof protocol.CommandMeta
         * @instance
         */
        CommandMeta.prototype.request_id = "";

        /**
         * CommandMeta expected_game_id.
         * @member {string} expected_game_id
         * @memberof protocol.CommandMeta
         * @instance
         */
        CommandMeta.prototype.expected_game_id = "";

        /**
         * CommandMeta expected_turn_id.
         * @member {number} expected_turn_id
         * @memberof protocol.CommandMeta
         * @instance
         */
        CommandMeta.prototype.expected_turn_id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified CommandMeta message. Does not implicitly {@link protocol.CommandMeta.verify|verify} messages.
         * @function encode
         * @memberof protocol.CommandMeta
         * @static
         * @param {protocol.CommandMeta.$Properties} message CommandMeta message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommandMeta.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id") && message.request_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.request_id);
            if (message.expected_game_id != null && $Object.hasOwnProperty.call(message, "expected_game_id") && message.expected_game_id !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.expected_game_id);
            if (message.expected_turn_id != null && $Object.hasOwnProperty.call(message, "expected_turn_id") && (typeof message.expected_turn_id === "object" ? message.expected_turn_id.low || message.expected_turn_id.high : message.expected_turn_id !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.expected_turn_id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a CommandMeta message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.CommandMeta
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.CommandMeta & protocol.CommandMeta.$Shape} CommandMeta
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommandMeta.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.CommandMeta(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.request_id = value;
                        else
                            delete message.request_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.expected_game_id = value;
                        else
                            delete message.expected_game_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expected_turn_id = value;
                        else
                            delete message.expected_turn_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a CommandMeta message.
         * @function verify
         * @memberof protocol.CommandMeta
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CommandMeta.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                if (!$util.isString(message.request_id))
                    return "request_id: string expected";
            if (message.expected_game_id != null && $Object.hasOwnProperty.call(message, "expected_game_id"))
                if (!$util.isString(message.expected_game_id))
                    return "expected_game_id: string expected";
            if (message.expected_turn_id != null && $Object.hasOwnProperty.call(message, "expected_turn_id"))
                if (!$util.isInteger(message.expected_turn_id) && !(message.expected_turn_id && $util.isInteger(message.expected_turn_id.low) && $util.isInteger(message.expected_turn_id.high)))
                    return "expected_turn_id: integer|Long expected";
            return null;
        };

        return CommandMeta;
    })();

    protocol.Message = (function() {

        /**
         * Properties of a Message.
         * @typedef {Object} protocol.Message.$Properties
         * @property {protocol.MessageType|null} [type] Message type
         * @property {Uint8Array|null} [payload] Message payload
         * @property {protocol.EventMeta.$Properties|null} [event] Message event
         * @property {protocol.CommandMeta.$Properties|null} [command] Message command
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Message.
         * @memberof protocol
         * @interface IMessage
         * @augments protocol.Message.$Properties
         * @deprecated Use protocol.Message.$Properties instead.
         */

        /**
         * Shape of a Message.
         * @typedef {protocol.Message.$Properties} protocol.Message.$Shape
         */

        /**
         * Constructs a new Message.
         * @memberof protocol
         * @classdesc Represents a Message.
         * @constructor
         * @param {protocol.Message.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Message = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Message type.
         * @member {protocol.MessageType} type
         * @memberof protocol.Message
         * @instance
         */
        Message.prototype.type = 0;

        /**
         * Message payload.
         * @member {Uint8Array} payload
         * @memberof protocol.Message
         * @instance
         */
        Message.prototype.payload = $util.newBuffer([]);

        /**
         * Message event.
         * @member {protocol.EventMeta.$Properties|null|undefined} event
         * @memberof protocol.Message
         * @instance
         */
        Message.prototype.event = null;

        /**
         * Message command.
         * @member {protocol.CommandMeta.$Properties|null|undefined} command
         * @memberof protocol.Message
         * @instance
         */
        Message.prototype.command = null;

        /**
         * Encodes the specified Message message. Does not implicitly {@link protocol.Message.verify|verify} messages.
         * @function encode
         * @memberof protocol.Message
         * @static
         * @param {protocol.Message.$Properties} message Message message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Message.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.payload != null && $Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payload);
            if (message.event != null && $Object.hasOwnProperty.call(message, "event"))
                $root.protocol.EventMeta.encode(message.event, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.command != null && $Object.hasOwnProperty.call(message, "command"))
                $root.protocol.CommandMeta.encode(message.command, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a Message message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.Message
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.Message & protocol.Message.$Shape} Message
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Message.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.Message(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.payload = value;
                        else
                            delete message.payload;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.event = $root.protocol.EventMeta.decode(reader, reader.uint32(), $undefined, _depth + 1, message.event);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.command = $root.protocol.CommandMeta.decode(reader, reader.uint32(), $undefined, _depth + 1, message.command);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a Message message.
         * @function verify
         * @memberof protocol.Message
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Message.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            if (message.payload != null && $Object.hasOwnProperty.call(message, "payload"))
                if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                    return "payload: buffer expected";
            if (message.event != null && $Object.hasOwnProperty.call(message, "event")) {
                let error = $root.protocol.EventMeta.verify(message.event, _depth + 1);
                if (error)
                    return "event." + error;
            }
            if (message.command != null && $Object.hasOwnProperty.call(message, "command")) {
                let error = $root.protocol.CommandMeta.verify(message.command, _depth + 1);
                if (error)
                    return "command." + error;
            }
            return null;
        };

        return Message;
    })();

    protocol.RoomCreatedPayload = (function() {

        /**
         * Properties of a RoomCreatedPayload.
         * @typedef {Object} protocol.RoomCreatedPayload.$Properties
         * @property {string|null} [room_code] RoomCreatedPayload room_code
         * @property {protocol.PlayerInfo.$Properties|null} [player] RoomCreatedPayload player
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomCreatedPayload.
         * @memberof protocol
         * @interface IRoomCreatedPayload
         * @augments protocol.RoomCreatedPayload.$Properties
         * @deprecated Use protocol.RoomCreatedPayload.$Properties instead.
         */

        /**
         * Shape of a RoomCreatedPayload.
         * @typedef {protocol.RoomCreatedPayload.$Properties} protocol.RoomCreatedPayload.$Shape
         */

        /**
         * Constructs a new RoomCreatedPayload.
         * @memberof protocol
         * @classdesc Represents a RoomCreatedPayload.
         * @constructor
         * @param {protocol.RoomCreatedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomCreatedPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomCreatedPayload room_code.
         * @member {string} room_code
         * @memberof protocol.RoomCreatedPayload
         * @instance
         */
        RoomCreatedPayload.prototype.room_code = "";

        /**
         * RoomCreatedPayload player.
         * @member {protocol.PlayerInfo.$Properties|null|undefined} player
         * @memberof protocol.RoomCreatedPayload
         * @instance
         */
        RoomCreatedPayload.prototype.player = null;

        /**
         * Encodes the specified RoomCreatedPayload message. Does not implicitly {@link protocol.RoomCreatedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.RoomCreatedPayload
         * @static
         * @param {protocol.RoomCreatedPayload.$Properties} message RoomCreatedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomCreatedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code") && message.room_code !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.room_code);
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.protocol.PlayerInfo.encode(message.player, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a RoomCreatedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.RoomCreatedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.RoomCreatedPayload & protocol.RoomCreatedPayload.$Shape} RoomCreatedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomCreatedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.RoomCreatedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.room_code = value;
                        else
                            delete message.room_code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.protocol.PlayerInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a RoomCreatedPayload message.
         * @function verify
         * @memberof protocol.RoomCreatedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomCreatedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code"))
                if (!$util.isString(message.room_code))
                    return "room_code: string expected";
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                let error = $root.protocol.PlayerInfo.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            return null;
        };

        return RoomCreatedPayload;
    })();

    protocol.RoomJoinedPayload = (function() {

        /**
         * Properties of a RoomJoinedPayload.
         * @typedef {Object} protocol.RoomJoinedPayload.$Properties
         * @property {string|null} [room_code] RoomJoinedPayload room_code
         * @property {protocol.PlayerInfo.$Properties|null} [player] RoomJoinedPayload player
         * @property {Array.<protocol.PlayerInfo.$Properties>|null} [players] RoomJoinedPayload players
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomJoinedPayload.
         * @memberof protocol
         * @interface IRoomJoinedPayload
         * @augments protocol.RoomJoinedPayload.$Properties
         * @deprecated Use protocol.RoomJoinedPayload.$Properties instead.
         */

        /**
         * Shape of a RoomJoinedPayload.
         * @typedef {protocol.RoomJoinedPayload.$Properties} protocol.RoomJoinedPayload.$Shape
         */

        /**
         * Constructs a new RoomJoinedPayload.
         * @memberof protocol
         * @classdesc Represents a RoomJoinedPayload.
         * @constructor
         * @param {protocol.RoomJoinedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomJoinedPayload = function (properties) {
            this.players = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomJoinedPayload room_code.
         * @member {string} room_code
         * @memberof protocol.RoomJoinedPayload
         * @instance
         */
        RoomJoinedPayload.prototype.room_code = "";

        /**
         * RoomJoinedPayload player.
         * @member {protocol.PlayerInfo.$Properties|null|undefined} player
         * @memberof protocol.RoomJoinedPayload
         * @instance
         */
        RoomJoinedPayload.prototype.player = null;

        /**
         * RoomJoinedPayload players.
         * @member {Array.<protocol.PlayerInfo.$Properties>} players
         * @memberof protocol.RoomJoinedPayload
         * @instance
         */
        RoomJoinedPayload.prototype.players = $util.emptyArray;

        /**
         * Encodes the specified RoomJoinedPayload message. Does not implicitly {@link protocol.RoomJoinedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.RoomJoinedPayload
         * @static
         * @param {protocol.RoomJoinedPayload.$Properties} message RoomJoinedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomJoinedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code") && message.room_code !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.room_code);
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.protocol.PlayerInfo.encode(message.player, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.protocol.PlayerInfo.encode(message.players[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a RoomJoinedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.RoomJoinedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.RoomJoinedPayload & protocol.RoomJoinedPayload.$Shape} RoomJoinedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomJoinedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.RoomJoinedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.room_code = value;
                        else
                            delete message.room_code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.protocol.PlayerInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.protocol.PlayerInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a RoomJoinedPayload message.
         * @function verify
         * @memberof protocol.RoomJoinedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomJoinedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.room_code != null && $Object.hasOwnProperty.call(message, "room_code"))
                if (!$util.isString(message.room_code))
                    return "room_code: string expected";
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                let error = $root.protocol.PlayerInfo.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            if (message.players != null && $Object.hasOwnProperty.call(message, "players")) {
                if (!$Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.protocol.PlayerInfo.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        return RoomJoinedPayload;
    })();

    protocol.PlayerJoinedPayload = (function() {

        /**
         * Properties of a PlayerJoinedPayload.
         * @typedef {Object} protocol.PlayerJoinedPayload.$Properties
         * @property {protocol.PlayerInfo.$Properties|null} [player] PlayerJoinedPayload player
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerJoinedPayload.
         * @memberof protocol
         * @interface IPlayerJoinedPayload
         * @augments protocol.PlayerJoinedPayload.$Properties
         * @deprecated Use protocol.PlayerJoinedPayload.$Properties instead.
         */

        /**
         * Shape of a PlayerJoinedPayload.
         * @typedef {protocol.PlayerJoinedPayload.$Properties} protocol.PlayerJoinedPayload.$Shape
         */

        /**
         * Constructs a new PlayerJoinedPayload.
         * @memberof protocol
         * @classdesc Represents a PlayerJoinedPayload.
         * @constructor
         * @param {protocol.PlayerJoinedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerJoinedPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerJoinedPayload player.
         * @member {protocol.PlayerInfo.$Properties|null|undefined} player
         * @memberof protocol.PlayerJoinedPayload
         * @instance
         */
        PlayerJoinedPayload.prototype.player = null;

        /**
         * Encodes the specified PlayerJoinedPayload message. Does not implicitly {@link protocol.PlayerJoinedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerJoinedPayload
         * @static
         * @param {protocol.PlayerJoinedPayload.$Properties} message PlayerJoinedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerJoinedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.protocol.PlayerInfo.encode(message.player, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerJoinedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerJoinedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerJoinedPayload & protocol.PlayerJoinedPayload.$Shape} PlayerJoinedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerJoinedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerJoinedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.protocol.PlayerInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerJoinedPayload message.
         * @function verify
         * @memberof protocol.PlayerJoinedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerJoinedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                let error = $root.protocol.PlayerInfo.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            return null;
        };

        return PlayerJoinedPayload;
    })();

    protocol.PlayerLeftPayload = (function() {

        /**
         * Properties of a PlayerLeftPayload.
         * @typedef {Object} protocol.PlayerLeftPayload.$Properties
         * @property {string|null} [player_id] PlayerLeftPayload player_id
         * @property {string|null} [player_name] PlayerLeftPayload player_name
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerLeftPayload.
         * @memberof protocol
         * @interface IPlayerLeftPayload
         * @augments protocol.PlayerLeftPayload.$Properties
         * @deprecated Use protocol.PlayerLeftPayload.$Properties instead.
         */

        /**
         * Shape of a PlayerLeftPayload.
         * @typedef {protocol.PlayerLeftPayload.$Properties} protocol.PlayerLeftPayload.$Shape
         */

        /**
         * Constructs a new PlayerLeftPayload.
         * @memberof protocol
         * @classdesc Represents a PlayerLeftPayload.
         * @constructor
         * @param {protocol.PlayerLeftPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerLeftPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerLeftPayload player_id.
         * @member {string} player_id
         * @memberof protocol.PlayerLeftPayload
         * @instance
         */
        PlayerLeftPayload.prototype.player_id = "";

        /**
         * PlayerLeftPayload player_name.
         * @member {string} player_name
         * @memberof protocol.PlayerLeftPayload
         * @instance
         */
        PlayerLeftPayload.prototype.player_name = "";

        /**
         * Encodes the specified PlayerLeftPayload message. Does not implicitly {@link protocol.PlayerLeftPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerLeftPayload
         * @static
         * @param {protocol.PlayerLeftPayload.$Properties} message PlayerLeftPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerLeftPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerLeftPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerLeftPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerLeftPayload & protocol.PlayerLeftPayload.$Shape} PlayerLeftPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerLeftPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerLeftPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerLeftPayload message.
         * @function verify
         * @memberof protocol.PlayerLeftPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerLeftPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            return null;
        };

        return PlayerLeftPayload;
    })();

    protocol.PlayerReadyPayload = (function() {

        /**
         * Properties of a PlayerReadyPayload.
         * @typedef {Object} protocol.PlayerReadyPayload.$Properties
         * @property {string|null} [player_id] PlayerReadyPayload player_id
         * @property {boolean|null} [ready] PlayerReadyPayload ready
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerReadyPayload.
         * @memberof protocol
         * @interface IPlayerReadyPayload
         * @augments protocol.PlayerReadyPayload.$Properties
         * @deprecated Use protocol.PlayerReadyPayload.$Properties instead.
         */

        /**
         * Shape of a PlayerReadyPayload.
         * @typedef {protocol.PlayerReadyPayload.$Properties} protocol.PlayerReadyPayload.$Shape
         */

        /**
         * Constructs a new PlayerReadyPayload.
         * @memberof protocol
         * @classdesc Represents a PlayerReadyPayload.
         * @constructor
         * @param {protocol.PlayerReadyPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerReadyPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerReadyPayload player_id.
         * @member {string} player_id
         * @memberof protocol.PlayerReadyPayload
         * @instance
         */
        PlayerReadyPayload.prototype.player_id = "";

        /**
         * PlayerReadyPayload ready.
         * @member {boolean} ready
         * @memberof protocol.PlayerReadyPayload
         * @instance
         */
        PlayerReadyPayload.prototype.ready = false;

        /**
         * Encodes the specified PlayerReadyPayload message. Does not implicitly {@link protocol.PlayerReadyPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerReadyPayload
         * @static
         * @param {protocol.PlayerReadyPayload.$Properties} message PlayerReadyPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerReadyPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.ready != null && $Object.hasOwnProperty.call(message, "ready") && message.ready !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.ready);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerReadyPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerReadyPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerReadyPayload & protocol.PlayerReadyPayload.$Shape} PlayerReadyPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerReadyPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerReadyPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ready = value;
                        else
                            delete message.ready;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerReadyPayload message.
         * @function verify
         * @memberof protocol.PlayerReadyPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerReadyPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.ready != null && $Object.hasOwnProperty.call(message, "ready"))
                if (typeof message.ready !== "boolean")
                    return "ready: boolean expected";
            return null;
        };

        return PlayerReadyPayload;
    })();

    protocol.GameStartPayload = (function() {

        /**
         * Properties of a GameStartPayload.
         * @typedef {Object} protocol.GameStartPayload.$Properties
         * @property {Array.<protocol.PlayerInfo.$Properties>|null} [players] GameStartPayload players
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GameStartPayload.
         * @memberof protocol
         * @interface IGameStartPayload
         * @augments protocol.GameStartPayload.$Properties
         * @deprecated Use protocol.GameStartPayload.$Properties instead.
         */

        /**
         * Shape of a GameStartPayload.
         * @typedef {protocol.GameStartPayload.$Properties} protocol.GameStartPayload.$Shape
         */

        /**
         * Constructs a new GameStartPayload.
         * @memberof protocol
         * @classdesc Represents a GameStartPayload.
         * @constructor
         * @param {protocol.GameStartPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GameStartPayload = function (properties) {
            this.players = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GameStartPayload players.
         * @member {Array.<protocol.PlayerInfo.$Properties>} players
         * @memberof protocol.GameStartPayload
         * @instance
         */
        GameStartPayload.prototype.players = $util.emptyArray;

        /**
         * Encodes the specified GameStartPayload message. Does not implicitly {@link protocol.GameStartPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.GameStartPayload
         * @static
         * @param {protocol.GameStartPayload.$Properties} message GameStartPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameStartPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.protocol.PlayerInfo.encode(message.players[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a GameStartPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.GameStartPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.GameStartPayload & protocol.GameStartPayload.$Shape} GameStartPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameStartPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.GameStartPayload();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.protocol.PlayerInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a GameStartPayload message.
         * @function verify
         * @memberof protocol.GameStartPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameStartPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.players != null && $Object.hasOwnProperty.call(message, "players")) {
                if (!$Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.protocol.PlayerInfo.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        return GameStartPayload;
    })();

    protocol.DealCardsPayload = (function() {

        /**
         * Properties of a DealCardsPayload.
         * @typedef {Object} protocol.DealCardsPayload.$Properties
         * @property {Array.<protocol.CardInfo.$Properties>|null} [cards] DealCardsPayload cards
         * @property {Array.<protocol.CardInfo.$Properties>|null} [bottom_cards] DealCardsPayload bottom_cards
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a DealCardsPayload.
         * @memberof protocol
         * @interface IDealCardsPayload
         * @augments protocol.DealCardsPayload.$Properties
         * @deprecated Use protocol.DealCardsPayload.$Properties instead.
         */

        /**
         * Shape of a DealCardsPayload.
         * @typedef {protocol.DealCardsPayload.$Properties} protocol.DealCardsPayload.$Shape
         */

        /**
         * Constructs a new DealCardsPayload.
         * @memberof protocol
         * @classdesc Represents a DealCardsPayload.
         * @constructor
         * @param {protocol.DealCardsPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const DealCardsPayload = function (properties) {
            this.cards = [];
            this.bottom_cards = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DealCardsPayload cards.
         * @member {Array.<protocol.CardInfo.$Properties>} cards
         * @memberof protocol.DealCardsPayload
         * @instance
         */
        DealCardsPayload.prototype.cards = $util.emptyArray;

        /**
         * DealCardsPayload bottom_cards.
         * @member {Array.<protocol.CardInfo.$Properties>} bottom_cards
         * @memberof protocol.DealCardsPayload
         * @instance
         */
        DealCardsPayload.prototype.bottom_cards = $util.emptyArray;

        /**
         * Encodes the specified DealCardsPayload message. Does not implicitly {@link protocol.DealCardsPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.DealCardsPayload
         * @static
         * @param {protocol.DealCardsPayload.$Properties} message DealCardsPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DealCardsPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.cards != null && message.cards.length)
                for (let i = 0; i < message.cards.length; ++i)
                    $root.protocol.CardInfo.encode(message.cards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.bottom_cards != null && message.bottom_cards.length)
                for (let i = 0; i < message.bottom_cards.length; ++i)
                    $root.protocol.CardInfo.encode(message.bottom_cards[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a DealCardsPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.DealCardsPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.DealCardsPayload & protocol.DealCardsPayload.$Shape} DealCardsPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DealCardsPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.DealCardsPayload();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bottom_cards && message.bottom_cards.length))
                            message.bottom_cards = [];
                        message.bottom_cards.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a DealCardsPayload message.
         * @function verify
         * @memberof protocol.DealCardsPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DealCardsPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.cards != null && $Object.hasOwnProperty.call(message, "cards")) {
                if (!$Array.isArray(message.cards))
                    return "cards: array expected";
                for (let i = 0; i < message.cards.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.cards[i], _depth + 1);
                    if (error)
                        return "cards." + error;
                }
            }
            if (message.bottom_cards != null && $Object.hasOwnProperty.call(message, "bottom_cards")) {
                if (!$Array.isArray(message.bottom_cards))
                    return "bottom_cards: array expected";
                for (let i = 0; i < message.bottom_cards.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.bottom_cards[i], _depth + 1);
                    if (error)
                        return "bottom_cards." + error;
                }
            }
            return null;
        };

        return DealCardsPayload;
    })();

    protocol.BidTurnPayload = (function() {

        /**
         * Properties of a BidTurnPayload.
         * @typedef {Object} protocol.BidTurnPayload.$Properties
         * @property {string|null} [player_id] BidTurnPayload player_id
         * @property {number|null} [timeout] BidTurnPayload timeout
         * @property {boolean|null} [is_grab] BidTurnPayload is_grab
         * @property {number|null} [multiplier] BidTurnPayload multiplier
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BidTurnPayload.
         * @memberof protocol
         * @interface IBidTurnPayload
         * @augments protocol.BidTurnPayload.$Properties
         * @deprecated Use protocol.BidTurnPayload.$Properties instead.
         */

        /**
         * Shape of a BidTurnPayload.
         * @typedef {protocol.BidTurnPayload.$Properties} protocol.BidTurnPayload.$Shape
         */

        /**
         * Constructs a new BidTurnPayload.
         * @memberof protocol
         * @classdesc Represents a BidTurnPayload.
         * @constructor
         * @param {protocol.BidTurnPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BidTurnPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BidTurnPayload player_id.
         * @member {string} player_id
         * @memberof protocol.BidTurnPayload
         * @instance
         */
        BidTurnPayload.prototype.player_id = "";

        /**
         * BidTurnPayload timeout.
         * @member {number} timeout
         * @memberof protocol.BidTurnPayload
         * @instance
         */
        BidTurnPayload.prototype.timeout = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BidTurnPayload is_grab.
         * @member {boolean} is_grab
         * @memberof protocol.BidTurnPayload
         * @instance
         */
        BidTurnPayload.prototype.is_grab = false;

        /**
         * BidTurnPayload multiplier.
         * @member {number} multiplier
         * @memberof protocol.BidTurnPayload
         * @instance
         */
        BidTurnPayload.prototype.multiplier = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified BidTurnPayload message. Does not implicitly {@link protocol.BidTurnPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.BidTurnPayload
         * @static
         * @param {protocol.BidTurnPayload.$Properties} message BidTurnPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BidTurnPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout") && (typeof message.timeout === "object" ? message.timeout.low || message.timeout.high : message.timeout !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timeout);
            if (message.is_grab != null && $Object.hasOwnProperty.call(message, "is_grab") && message.is_grab !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.is_grab);
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier") && (typeof message.multiplier === "object" ? message.multiplier.low || message.multiplier.high : message.multiplier !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.multiplier);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a BidTurnPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.BidTurnPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.BidTurnPayload & protocol.BidTurnPayload.$Shape} BidTurnPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BidTurnPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.BidTurnPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.timeout = value;
                        else
                            delete message.timeout;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_grab = value;
                        else
                            delete message.is_grab;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.multiplier = value;
                        else
                            delete message.multiplier;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a BidTurnPayload message.
         * @function verify
         * @memberof protocol.BidTurnPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BidTurnPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                if (!$util.isInteger(message.timeout) && !(message.timeout && $util.isInteger(message.timeout.low) && $util.isInteger(message.timeout.high)))
                    return "timeout: integer|Long expected";
            if (message.is_grab != null && $Object.hasOwnProperty.call(message, "is_grab"))
                if (typeof message.is_grab !== "boolean")
                    return "is_grab: boolean expected";
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier"))
                if (!$util.isInteger(message.multiplier) && !(message.multiplier && $util.isInteger(message.multiplier.low) && $util.isInteger(message.multiplier.high)))
                    return "multiplier: integer|Long expected";
            return null;
        };

        return BidTurnPayload;
    })();

    protocol.BidResultPayload = (function() {

        /**
         * Properties of a BidResultPayload.
         * @typedef {Object} protocol.BidResultPayload.$Properties
         * @property {string|null} [player_id] BidResultPayload player_id
         * @property {string|null} [player_name] BidResultPayload player_name
         * @property {boolean|null} [bid] BidResultPayload bid
         * @property {boolean|null} [is_grab] BidResultPayload is_grab
         * @property {number|null} [multiplier] BidResultPayload multiplier
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BidResultPayload.
         * @memberof protocol
         * @interface IBidResultPayload
         * @augments protocol.BidResultPayload.$Properties
         * @deprecated Use protocol.BidResultPayload.$Properties instead.
         */

        /**
         * Shape of a BidResultPayload.
         * @typedef {protocol.BidResultPayload.$Properties} protocol.BidResultPayload.$Shape
         */

        /**
         * Constructs a new BidResultPayload.
         * @memberof protocol
         * @classdesc Represents a BidResultPayload.
         * @constructor
         * @param {protocol.BidResultPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BidResultPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BidResultPayload player_id.
         * @member {string} player_id
         * @memberof protocol.BidResultPayload
         * @instance
         */
        BidResultPayload.prototype.player_id = "";

        /**
         * BidResultPayload player_name.
         * @member {string} player_name
         * @memberof protocol.BidResultPayload
         * @instance
         */
        BidResultPayload.prototype.player_name = "";

        /**
         * BidResultPayload bid.
         * @member {boolean} bid
         * @memberof protocol.BidResultPayload
         * @instance
         */
        BidResultPayload.prototype.bid = false;

        /**
         * BidResultPayload is_grab.
         * @member {boolean} is_grab
         * @memberof protocol.BidResultPayload
         * @instance
         */
        BidResultPayload.prototype.is_grab = false;

        /**
         * BidResultPayload multiplier.
         * @member {number} multiplier
         * @memberof protocol.BidResultPayload
         * @instance
         */
        BidResultPayload.prototype.multiplier = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified BidResultPayload message. Does not implicitly {@link protocol.BidResultPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.BidResultPayload
         * @static
         * @param {protocol.BidResultPayload.$Properties} message BidResultPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BidResultPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.bid != null && $Object.hasOwnProperty.call(message, "bid") && message.bid !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.bid);
            if (message.is_grab != null && $Object.hasOwnProperty.call(message, "is_grab") && message.is_grab !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.is_grab);
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier") && (typeof message.multiplier === "object" ? message.multiplier.low || message.multiplier.high : message.multiplier !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.multiplier);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a BidResultPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.BidResultPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.BidResultPayload & protocol.BidResultPayload.$Shape} BidResultPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BidResultPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.BidResultPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.bid = value;
                        else
                            delete message.bid;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_grab = value;
                        else
                            delete message.is_grab;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.multiplier = value;
                        else
                            delete message.multiplier;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a BidResultPayload message.
         * @function verify
         * @memberof protocol.BidResultPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BidResultPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.bid != null && $Object.hasOwnProperty.call(message, "bid"))
                if (typeof message.bid !== "boolean")
                    return "bid: boolean expected";
            if (message.is_grab != null && $Object.hasOwnProperty.call(message, "is_grab"))
                if (typeof message.is_grab !== "boolean")
                    return "is_grab: boolean expected";
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier"))
                if (!$util.isInteger(message.multiplier) && !(message.multiplier && $util.isInteger(message.multiplier.low) && $util.isInteger(message.multiplier.high)))
                    return "multiplier: integer|Long expected";
            return null;
        };

        return BidResultPayload;
    })();

    protocol.LandlordPayload = (function() {

        /**
         * Properties of a LandlordPayload.
         * @typedef {Object} protocol.LandlordPayload.$Properties
         * @property {string|null} [player_id] LandlordPayload player_id
         * @property {string|null} [player_name] LandlordPayload player_name
         * @property {Array.<protocol.CardInfo.$Properties>|null} [bottom_cards] LandlordPayload bottom_cards
         * @property {number|null} [multiplier] LandlordPayload multiplier
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LandlordPayload.
         * @memberof protocol
         * @interface ILandlordPayload
         * @augments protocol.LandlordPayload.$Properties
         * @deprecated Use protocol.LandlordPayload.$Properties instead.
         */

        /**
         * Shape of a LandlordPayload.
         * @typedef {protocol.LandlordPayload.$Properties} protocol.LandlordPayload.$Shape
         */

        /**
         * Constructs a new LandlordPayload.
         * @memberof protocol
         * @classdesc Represents a LandlordPayload.
         * @constructor
         * @param {protocol.LandlordPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LandlordPayload = function (properties) {
            this.bottom_cards = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LandlordPayload player_id.
         * @member {string} player_id
         * @memberof protocol.LandlordPayload
         * @instance
         */
        LandlordPayload.prototype.player_id = "";

        /**
         * LandlordPayload player_name.
         * @member {string} player_name
         * @memberof protocol.LandlordPayload
         * @instance
         */
        LandlordPayload.prototype.player_name = "";

        /**
         * LandlordPayload bottom_cards.
         * @member {Array.<protocol.CardInfo.$Properties>} bottom_cards
         * @memberof protocol.LandlordPayload
         * @instance
         */
        LandlordPayload.prototype.bottom_cards = $util.emptyArray;

        /**
         * LandlordPayload multiplier.
         * @member {number} multiplier
         * @memberof protocol.LandlordPayload
         * @instance
         */
        LandlordPayload.prototype.multiplier = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Encodes the specified LandlordPayload message. Does not implicitly {@link protocol.LandlordPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.LandlordPayload
         * @static
         * @param {protocol.LandlordPayload.$Properties} message LandlordPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LandlordPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.bottom_cards != null && message.bottom_cards.length)
                for (let i = 0; i < message.bottom_cards.length; ++i)
                    $root.protocol.CardInfo.encode(message.bottom_cards[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier") && (typeof message.multiplier === "object" ? message.multiplier.low || message.multiplier.high : message.multiplier !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.multiplier);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a LandlordPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.LandlordPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.LandlordPayload & protocol.LandlordPayload.$Shape} LandlordPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LandlordPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.LandlordPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bottom_cards && message.bottom_cards.length))
                            message.bottom_cards = [];
                        message.bottom_cards.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.multiplier = value;
                        else
                            delete message.multiplier;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a LandlordPayload message.
         * @function verify
         * @memberof protocol.LandlordPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LandlordPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.bottom_cards != null && $Object.hasOwnProperty.call(message, "bottom_cards")) {
                if (!$Array.isArray(message.bottom_cards))
                    return "bottom_cards: array expected";
                for (let i = 0; i < message.bottom_cards.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.bottom_cards[i], _depth + 1);
                    if (error)
                        return "bottom_cards." + error;
                }
            }
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier"))
                if (!$util.isInteger(message.multiplier) && !(message.multiplier && $util.isInteger(message.multiplier.low) && $util.isInteger(message.multiplier.high)))
                    return "multiplier: integer|Long expected";
            return null;
        };

        return LandlordPayload;
    })();

    protocol.PlayTurnPayload = (function() {

        /**
         * Properties of a PlayTurnPayload.
         * @typedef {Object} protocol.PlayTurnPayload.$Properties
         * @property {string|null} [player_id] PlayTurnPayload player_id
         * @property {number|null} [timeout] PlayTurnPayload timeout
         * @property {boolean|null} [must_play] PlayTurnPayload must_play
         * @property {boolean|null} [can_beat] PlayTurnPayload can_beat
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayTurnPayload.
         * @memberof protocol
         * @interface IPlayTurnPayload
         * @augments protocol.PlayTurnPayload.$Properties
         * @deprecated Use protocol.PlayTurnPayload.$Properties instead.
         */

        /**
         * Shape of a PlayTurnPayload.
         * @typedef {protocol.PlayTurnPayload.$Properties} protocol.PlayTurnPayload.$Shape
         */

        /**
         * Constructs a new PlayTurnPayload.
         * @memberof protocol
         * @classdesc Represents a PlayTurnPayload.
         * @constructor
         * @param {protocol.PlayTurnPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayTurnPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayTurnPayload player_id.
         * @member {string} player_id
         * @memberof protocol.PlayTurnPayload
         * @instance
         */
        PlayTurnPayload.prototype.player_id = "";

        /**
         * PlayTurnPayload timeout.
         * @member {number} timeout
         * @memberof protocol.PlayTurnPayload
         * @instance
         */
        PlayTurnPayload.prototype.timeout = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayTurnPayload must_play.
         * @member {boolean} must_play
         * @memberof protocol.PlayTurnPayload
         * @instance
         */
        PlayTurnPayload.prototype.must_play = false;

        /**
         * PlayTurnPayload can_beat.
         * @member {boolean} can_beat
         * @memberof protocol.PlayTurnPayload
         * @instance
         */
        PlayTurnPayload.prototype.can_beat = false;

        /**
         * Encodes the specified PlayTurnPayload message. Does not implicitly {@link protocol.PlayTurnPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayTurnPayload
         * @static
         * @param {protocol.PlayTurnPayload.$Properties} message PlayTurnPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayTurnPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout") && (typeof message.timeout === "object" ? message.timeout.low || message.timeout.high : message.timeout !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timeout);
            if (message.must_play != null && $Object.hasOwnProperty.call(message, "must_play") && message.must_play !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.must_play);
            if (message.can_beat != null && $Object.hasOwnProperty.call(message, "can_beat") && message.can_beat !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.can_beat);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayTurnPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayTurnPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayTurnPayload & protocol.PlayTurnPayload.$Shape} PlayTurnPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayTurnPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayTurnPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.timeout = value;
                        else
                            delete message.timeout;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.must_play = value;
                        else
                            delete message.must_play;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.can_beat = value;
                        else
                            delete message.can_beat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayTurnPayload message.
         * @function verify
         * @memberof protocol.PlayTurnPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayTurnPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                if (!$util.isInteger(message.timeout) && !(message.timeout && $util.isInteger(message.timeout.low) && $util.isInteger(message.timeout.high)))
                    return "timeout: integer|Long expected";
            if (message.must_play != null && $Object.hasOwnProperty.call(message, "must_play"))
                if (typeof message.must_play !== "boolean")
                    return "must_play: boolean expected";
            if (message.can_beat != null && $Object.hasOwnProperty.call(message, "can_beat"))
                if (typeof message.can_beat !== "boolean")
                    return "can_beat: boolean expected";
            return null;
        };

        return PlayTurnPayload;
    })();

    protocol.CardPlayedPayload = (function() {

        /**
         * Properties of a CardPlayedPayload.
         * @typedef {Object} protocol.CardPlayedPayload.$Properties
         * @property {string|null} [player_id] CardPlayedPayload player_id
         * @property {string|null} [player_name] CardPlayedPayload player_name
         * @property {Array.<protocol.CardInfo.$Properties>|null} [cards] CardPlayedPayload cards
         * @property {number|null} [cards_left] CardPlayedPayload cards_left
         * @property {string|null} [hand_type] CardPlayedPayload hand_type
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CardPlayedPayload.
         * @memberof protocol
         * @interface ICardPlayedPayload
         * @augments protocol.CardPlayedPayload.$Properties
         * @deprecated Use protocol.CardPlayedPayload.$Properties instead.
         */

        /**
         * Shape of a CardPlayedPayload.
         * @typedef {protocol.CardPlayedPayload.$Properties} protocol.CardPlayedPayload.$Shape
         */

        /**
         * Constructs a new CardPlayedPayload.
         * @memberof protocol
         * @classdesc Represents a CardPlayedPayload.
         * @constructor
         * @param {protocol.CardPlayedPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CardPlayedPayload = function (properties) {
            this.cards = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CardPlayedPayload player_id.
         * @member {string} player_id
         * @memberof protocol.CardPlayedPayload
         * @instance
         */
        CardPlayedPayload.prototype.player_id = "";

        /**
         * CardPlayedPayload player_name.
         * @member {string} player_name
         * @memberof protocol.CardPlayedPayload
         * @instance
         */
        CardPlayedPayload.prototype.player_name = "";

        /**
         * CardPlayedPayload cards.
         * @member {Array.<protocol.CardInfo.$Properties>} cards
         * @memberof protocol.CardPlayedPayload
         * @instance
         */
        CardPlayedPayload.prototype.cards = $util.emptyArray;

        /**
         * CardPlayedPayload cards_left.
         * @member {number} cards_left
         * @memberof protocol.CardPlayedPayload
         * @instance
         */
        CardPlayedPayload.prototype.cards_left = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CardPlayedPayload hand_type.
         * @member {string} hand_type
         * @memberof protocol.CardPlayedPayload
         * @instance
         */
        CardPlayedPayload.prototype.hand_type = "";

        /**
         * Encodes the specified CardPlayedPayload message. Does not implicitly {@link protocol.CardPlayedPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.CardPlayedPayload
         * @static
         * @param {protocol.CardPlayedPayload.$Properties} message CardPlayedPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CardPlayedPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.cards != null && message.cards.length)
                for (let i = 0; i < message.cards.length; ++i)
                    $root.protocol.CardInfo.encode(message.cards[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.cards_left != null && $Object.hasOwnProperty.call(message, "cards_left") && (typeof message.cards_left === "object" ? message.cards_left.low || message.cards_left.high : message.cards_left !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.cards_left);
            if (message.hand_type != null && $Object.hasOwnProperty.call(message, "hand_type") && message.hand_type !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.hand_type);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a CardPlayedPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.CardPlayedPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.CardPlayedPayload & protocol.CardPlayedPayload.$Shape} CardPlayedPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CardPlayedPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.CardPlayedPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.protocol.CardInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.cards_left = value;
                        else
                            delete message.cards_left;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hand_type = value;
                        else
                            delete message.hand_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a CardPlayedPayload message.
         * @function verify
         * @memberof protocol.CardPlayedPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CardPlayedPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            if (message.cards != null && $Object.hasOwnProperty.call(message, "cards")) {
                if (!$Array.isArray(message.cards))
                    return "cards: array expected";
                for (let i = 0; i < message.cards.length; ++i) {
                    let error = $root.protocol.CardInfo.verify(message.cards[i], _depth + 1);
                    if (error)
                        return "cards." + error;
                }
            }
            if (message.cards_left != null && $Object.hasOwnProperty.call(message, "cards_left"))
                if (!$util.isInteger(message.cards_left) && !(message.cards_left && $util.isInteger(message.cards_left.low) && $util.isInteger(message.cards_left.high)))
                    return "cards_left: integer|Long expected";
            if (message.hand_type != null && $Object.hasOwnProperty.call(message, "hand_type"))
                if (!$util.isString(message.hand_type))
                    return "hand_type: string expected";
            return null;
        };

        return CardPlayedPayload;
    })();

    protocol.PlayerPassPayload = (function() {

        /**
         * Properties of a PlayerPassPayload.
         * @typedef {Object} protocol.PlayerPassPayload.$Properties
         * @property {string|null} [player_id] PlayerPassPayload player_id
         * @property {string|null} [player_name] PlayerPassPayload player_name
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerPassPayload.
         * @memberof protocol
         * @interface IPlayerPassPayload
         * @augments protocol.PlayerPassPayload.$Properties
         * @deprecated Use protocol.PlayerPassPayload.$Properties instead.
         */

        /**
         * Shape of a PlayerPassPayload.
         * @typedef {protocol.PlayerPassPayload.$Properties} protocol.PlayerPassPayload.$Shape
         */

        /**
         * Constructs a new PlayerPassPayload.
         * @memberof protocol
         * @classdesc Represents a PlayerPassPayload.
         * @constructor
         * @param {protocol.PlayerPassPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerPassPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerPassPayload player_id.
         * @member {string} player_id
         * @memberof protocol.PlayerPassPayload
         * @instance
         */
        PlayerPassPayload.prototype.player_id = "";

        /**
         * PlayerPassPayload player_name.
         * @member {string} player_name
         * @memberof protocol.PlayerPassPayload
         * @instance
         */
        PlayerPassPayload.prototype.player_name = "";

        /**
         * Encodes the specified PlayerPassPayload message. Does not implicitly {@link protocol.PlayerPassPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.PlayerPassPayload
         * @static
         * @param {protocol.PlayerPassPayload.$Properties} message PlayerPassPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerPassPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id") && message.player_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.player_id);
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name") && message.player_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.player_name);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a PlayerPassPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.PlayerPassPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.PlayerPassPayload & protocol.PlayerPassPayload.$Shape} PlayerPassPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerPassPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.PlayerPassPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_id = value;
                        else
                            delete message.player_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.player_name = value;
                        else
                            delete message.player_name;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a PlayerPassPayload message.
         * @function verify
         * @memberof protocol.PlayerPassPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerPassPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
                if (!$util.isString(message.player_id))
                    return "player_id: string expected";
            if (message.player_name != null && $Object.hasOwnProperty.call(message, "player_name"))
                if (!$util.isString(message.player_name))
                    return "player_name: string expected";
            return null;
        };

        return PlayerPassPayload;
    })();

    protocol.GameOverPayload = (function() {

        /**
         * Properties of a GameOverPayload.
         * @typedef {Object} protocol.GameOverPayload.$Properties
         * @property {string|null} [winner_id] GameOverPayload winner_id
         * @property {string|null} [winner_name] GameOverPayload winner_name
         * @property {boolean|null} [is_landlord] GameOverPayload is_landlord
         * @property {Array.<protocol.PlayerHand.$Properties>|null} [player_hands] GameOverPayload player_hands
         * @property {number|null} [multiplier] GameOverPayload multiplier
         * @property {Array.<protocol.PlayerScore.$Properties>|null} [scores] GameOverPayload scores
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GameOverPayload.
         * @memberof protocol
         * @interface IGameOverPayload
         * @augments protocol.GameOverPayload.$Properties
         * @deprecated Use protocol.GameOverPayload.$Properties instead.
         */

        /**
         * Shape of a GameOverPayload.
         * @typedef {protocol.GameOverPayload.$Properties} protocol.GameOverPayload.$Shape
         */

        /**
         * Constructs a new GameOverPayload.
         * @memberof protocol
         * @classdesc Represents a GameOverPayload.
         * @constructor
         * @param {protocol.GameOverPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GameOverPayload = function (properties) {
            this.player_hands = [];
            this.scores = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GameOverPayload winner_id.
         * @member {string} winner_id
         * @memberof protocol.GameOverPayload
         * @instance
         */
        GameOverPayload.prototype.winner_id = "";

        /**
         * GameOverPayload winner_name.
         * @member {string} winner_name
         * @memberof protocol.GameOverPayload
         * @instance
         */
        GameOverPayload.prototype.winner_name = "";

        /**
         * GameOverPayload is_landlord.
         * @member {boolean} is_landlord
         * @memberof protocol.GameOverPayload
         * @instance
         */
        GameOverPayload.prototype.is_landlord = false;

        /**
         * GameOverPayload player_hands.
         * @member {Array.<protocol.PlayerHand.$Properties>} player_hands
         * @memberof protocol.GameOverPayload
         * @instance
         */
        GameOverPayload.prototype.player_hands = $util.emptyArray;

        /**
         * GameOverPayload multiplier.
         * @member {number} multiplier
         * @memberof protocol.GameOverPayload
         * @instance
         */
        GameOverPayload.prototype.multiplier = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GameOverPayload scores.
         * @member {Array.<protocol.PlayerScore.$Properties>} scores
         * @memberof protocol.GameOverPayload
         * @instance
         */
        GameOverPayload.prototype.scores = $util.emptyArray;

        /**
         * Encodes the specified GameOverPayload message. Does not implicitly {@link protocol.GameOverPayload.verify|verify} messages.
         * @function encode
         * @memberof protocol.GameOverPayload
         * @static
         * @param {protocol.GameOverPayload.$Properties} message GameOverPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameOverPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.winner_id != null && $Object.hasOwnProperty.call(message, "winner_id") && message.winner_id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.winner_id);
            if (message.winner_name != null && $Object.hasOwnProperty.call(message, "winner_name") && message.winner_name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.winner_name);
            if (message.is_landlord != null && $Object.hasOwnProperty.call(message, "is_landlord") && message.is_landlord !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.is_landlord);
            if (message.player_hands != null && message.player_hands.length)
                for (let i = 0; i < message.player_hands.length; ++i)
                    $root.protocol.PlayerHand.encode(message.player_hands[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier") && (typeof message.multiplier === "object" ? message.multiplier.low || message.multiplier.high : message.multiplier !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.multiplier);
            if (message.scores != null && message.scores.length)
                for (let i = 0; i < message.scores.length; ++i)
                    $root.protocol.PlayerScore.encode(message.scores[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Decodes a GameOverPayload message from the specified reader or buffer.
         * @function decode
         * @memberof protocol.GameOverPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {protocol.GameOverPayload & protocol.GameOverPayload.$Shape} GameOverPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameOverPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.protocol.GameOverPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.winner_id = value;
                        else
                            delete message.winner_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.winner_name = value;
                        else
                            delete message.winner_name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_landlord = value;
                        else
                            delete message.is_landlord;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.player_hands && message.player_hands.length))
                            message.player_hands = [];
                        message.player_hands.push($root.protocol.PlayerHand.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.multiplier = value;
                        else
                            delete message.multiplier;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push($root.protocol.PlayerScore.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Verifies a GameOverPayload message.
         * @function verify
         * @memberof protocol.GameOverPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameOverPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.winner_id != null && $Object.hasOwnProperty.call(message, "winner_id"))
                if (!$util.isString(message.winner_id))
                    return "winner_id: string expected";
            if (message.winner_name != null && $Object.hasOwnProperty.call(message, "winner_name"))
                if (!$util.isString(message.winner_name))
                    return "winner_name: string expected";
            if (message.is_landlord != null && $Object.hasOwnProperty.call(message, "is_landlord"))
                if (typeof message.is_landlord !== "boolean")
                    return "is_landlord: boolean expected";
            if (message.player_hands != null && $Object.hasOwnProperty.call(message, "player_hands")) {
                if (!$Array.isArray(message.player_hands))
                    return "player_hands: array expected";
                for (let i = 0; i < message.player_hands.length; ++i) {
                    let error = $root.protocol.PlayerHand.verify(message.player_hands[i], _depth + 1);
                    if (error)
                        return "player_hands." + error;
                }
            }
            if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier"))
                if (!$util.isInteger(message.multiplier) && !(message.multiplier && $util.isInteger(message.multiplier.low) && $util.isInteger(message.multiplier.high)))
                    return "multiplier: integer|Long expected";
            if (message.scores != null && $Object.hasOwnProperty.call(message, "scores")) {
                if (!$Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i) {
                    let error = $root.protocol.PlayerScore.verify(message.scores[i], _depth + 1);
                    if (error)
                        return "scores." + error;
                }
            }
            return null;
        };

        return GameOverPayload;
    })();

    return protocol;
})();

export {
  $root as default
};
